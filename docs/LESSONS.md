# Licoes Aprendidas

Conhecimento reutilizavel extraido do desenvolvimento do Saira.
Indexado por **tema** -- consulte antes de implementar algo similar.

---

## R Package Structure

- **Linhas vazias no DESCRIPTION** quebram `pkgload::load_all()`. Todo campo deve seguir imediatamente o anterior.
- **Todo pacote usado com `::`** precisa estar no `Imports` do DESCRIPTION. Exemplo: `here`, `jsonlite`.
- **`pkgload::load_all()`** carrega todos os arquivos de `R/` automaticamente -- nao precisa de `source()` em cada modulo.
- **Diretorio de trabalho**: `pkgload::load_all()` precisa ser chamado a partir do diretorio que contem `DESCRIPTION`.
- **Rename de pacote sem compat legada exige varredura por string literal**: atualizar `Package`, `library()`, `test_check()`, `system.file(package=...)`, `asNamespace()` e `getFromNamespace()` no mesmo ciclo evita ambiente parcialmente quebrado.
- **Sessao R com namespace defasado apos update sem reiniciar**: ao atualizar o pacote (via `pak`/reinstalar) com o R aberto, a sessao continua executando o namespace antigo ja carregado na memoria enquanto o disco ja tem a versao nova -- o app "parece" a versao velha (foi a causa-raiz de um falso "cache de mapeamento preso na 0.3.0"). Detectar comparando `getNamespaceVersion("saira")` (carregado na sessao) com `utils::packageVersion("saira")` (le o DESCRIPTION no disco); sob `pkgload::load_all()` ambos batem, entao dev nao dispara falso positivo. Implementado em `saira_session_is_stale()`/`notify_session_stale()` (aviso fixo no app + `warning()` no console) mais um badge de versao no navbar para confirmacao visual.

## Shiny / Reactive Patterns

- **`uiOutput` fica vazio no carregamento inicial**: o container HTML existe no DOM desde o primeiro render, mas o conteudo so chega apos o servidor processar o `renderUI` e enviar via WebSocket. Para titulos de nav e outros elementos proeminentes, incluir texto estatico como fallback visual e esconde-lo via CSS (`:has(:not(:empty))`) elimina o flash de branco sem remover a reatividade de idioma.
- **`renderUI` recria inputs** a cada invalidacao. Usar `isolate(input$...)` para preservar valores do usuario entre re-renders.
- **Um default nao-restaurado num input recriado nao "perde" o valor: ele impoe outro** (issue #110). O painel de Generalizacao era um `renderUI` unico que lia o frame mapeado, entao qualquer edicao no Mapeamento o invalidava; ao voltar para a aba, o `radioButtons` de `sensitive_mode` renascia com `selected = "publish"` fixo, o cliente ecoava `publish` e o payload virava `enabled = FALSE` — o ZIP saia com as coordenadas exatas de especies ameacadas, sem aviso. Radio e checkbox sao os casos perigosos: um campo de texto recriado vazio o usuario ve, um radio recriado com a opcao errada parece uma escolha. **Regra**: em input recriado por `renderUI`, todo `selected`/`value` restaura estado; se um handler de reset limpa esses inputs via `update*Input`, restaurar de um espelho `reactiveVal` e nao de `input$` — o espelho e limpo no mesmo flush, o `input$` so um flush depois. Ler o espelho sob `isolate()`, ou o painel re-renderiza a cada clique.
- **Controles externos de DataTable renderizados via `renderUI`** devem usar eventos delegados (`$(document).on(...)`) com namespace por instancia; binding direto no elemento tende a quebrar busca/filtros quando o DOM e recriado.
- **Checkboxes de selecao unica**: `checkboxGroupInput` + `observeEvent` no server para forcar single-select. Mantem a estetica de checkbox quadrado.
- **Loops bloqueantes** em Shiny sincrono nao atualizam UI durante execucao. Progresso real requer HTML estatico + atualizacao JS client-side.
- **`shiny::toJSON`** pode nao estar disponivel em todas as versoes. Preferir `jsonlite::toJSON(...)` com dependencia declarada.
- **`uiOutput` em `placeholder`** de `fileInput` nao funciona. Usar `update*` no server.
- **Inputs dinamicos em paginas/modal**: evitar `observe` continuo lendo todos os `input$*` do conjunto; sincronizar estado por snapshot em eventos explicitos (ex.: `Salvar`, `Anterior`, `Proxima`) reduz cascata reativa.
- **`downloadButton` com label dinamico**: evitar `uiOutput` aninhado dentro do proprio `downloadButton`; renderizar o botao completo via `renderUI` em um container dedicado.
- **`downloadButton` ja tem icone padrao**: ao montar label com `icon("download")`, definir `icon = NULL` ou usar label textual + argumento `icon` explicito para evitar duplicacao visual.
- **Confirmacao antes de download**: para modal de confirmacao/progresso, usar `actionButton` visivel e acionar um `downloadButton` oculto via `sendCustomMessage` preserva o `downloadHandler` sem quebrar UX.
- **Validacao taxonomica longa tambem precisa de modal bloqueante**: em fluxos com `taxadb`, usar barra/fases estimadas client-side no padrao Rostrum evita percepcao de travamento.
- **Evitar `req()` silencioso em handlers de acao (`observeEvent`)**: em botoes de validacao/export, prefira checagem explicita com `showNotification` para nao aparentar "clique sem efeito" quando o reactive upstream estiver vazio.
- **Nao conectar output de visualizacao rapida a reactive de processamento completo**: se a tabela de preview depender do mesmo reactive usado para export completo, qualquer remapeamento pode bloquear a UX. **Regressao recorrente** (v0.2.5 -> v0.2.6, ADR-094): o card de mascaramento de especies sensiveis em `mod_preview.R` voltou a chamar `download_data_r()` (= `processed_data_r`, full mapping pipeline com 217 DwC terms) so para contar quantos registros sensiveis o dataset tinha. Em datasets reais (~9k registros sensiveis, dataset total ainda maior) qualquer troca de aba apos mapear `scientificName` ficava ~1 min bloqueada. Fix: introduzir uma reativa-projecao leve em `mod_mapping.R` (`sensitive_overview_input_r`: pega so `scientificName + decimalLatitude + decimalLongitude` do `raw_data + map_values`, sem rodar `build_mapped_result`), expor via `mapping_result` named list e consumir no `mod_preview_server`. Mesmo padrao dos demais gates leves do `mod_mapping`. **Checklist de revisao**: se uma reativa do Preview chama `download_data_r()`/`processed_data_r()`/`mapped_data_r()` so para resumir/contar/agregar uma fracao do dataset, ela esta no caminho errado — derive uma projecao a partir do raw + map_values.
- **Separar canais reativos por intencao de uso**: manter um `reactive` leve para UI (`head(...)`) e um `reactive` completo para acoes explicitas (download/validacao) reduz latencia percebida sem perder fidelidade no arquivo final.
- **Nao usar `mapped_data` completo para habilitar botao de acao**: checagens de prontidao de UI (ex.: habilitar `Validar nomes`) devem depender de sinal leve do modulo de origem, nao de pipeline completo.
- **Gates reativos leves evitam latencia na troca de abas**: apos mapear `scientificName`, voltar para a aba de validacao pode ficar lento se `quick_inputs()` materializar `processed_data`; um gate com `status = no_data/missing_scientific/ok` elimina esse acoplamento.
- **Para validacoes distintas, use gates leves distintos**: compartilhar gate de nomes na aba de coordenadas gera contrato ambiguo; prefira um gate dedicado (`validation_gate_coords`) com campos especificos do dominio.
- **Compatibilidade progressiva em modulos Shiny**: ao introduzir canal reativo leve novo (`validation_gate_r`), manter fallback legado para `mapped_data_r()` evita quebrar chamadas antigas de `mod_validate_names_server`.
- **Gate leve nao pode quebrar quando upstream usa `req()`**: se o gate consome reactive de upload (`raw_data_r`) que faz `req(input$file)`, capturar `shiny.silent.error` e retornar estado estavel (`no_data`) evita UI em branco antes do upload.
- **Em encadeamento por flags (`start_requested`/`run_requested`), `ignoreInit = TRUE` pode suprimir o primeiro ciclo util**: quando a flag muda de `FALSE -> TRUE` pela primeira vez, o observer pode nao executar em ambiente de teste. Para eventos internos de orquestracao, preferir `ignoreInit = FALSE`.
- **`session$onFlushed()` nem sempre e a melhor ancora para teste de servidor**: para fluxos de execucao sincrona em `testServer`, disparo direto de estado costuma ser mais estavel que depender de flush de UI.
- **Quando o modulo define filtro pos-execucao por padrao (ex.: `problems`)**, os testes devem refletir esse contrato antes de afirmar contagem de `all`.
- **Feedback imediato em clique + trabalho pesado**: em `observeEvent` de acao longa, usar estrategia em duas fases (`starting` agora + processamento pesado no proximo flush com `session$onFlushed`) evita o efeito de "cliquei e nada aconteceu".
- **Filtro de stream sensivel a status deve ser server-side quando afeta decisao de UX**: manter `stream_filter` em `reactiveValues` evita divergencia entre estado visual e estado de dados (dispensa `runjs` para regra de negocio).
- **Quando houver estado pre-validacao sem dados, substituir empty-state por instrucoes acionaveis** melhora escaneabilidade sem tocar na logica de execucao.
- **Assets estaticos CSS podem ficar stale no browser** em iteracoes de UI. Adicionar versionamento no `href` (`custom.css?v=...`) reduz falso negativo de validacao visual.
- **Dropzone custom sobre `fileInput` deve preservar o input real**: se for remover a caixa nativa (`.input-group`), reanexar o `input[type=file]` no container custom e manter `dispatchEvent("change")` garante compatibilidade com o binding do Shiny.
- **`dataTransfer.types` varia entre navegadores**: para detectar arquivos no drag-and-drop, usar fallback em cadeia (`contains`, `indexOf`, `item`) evita regressao silenciosa quando `includes` nao esta disponivel.
- **Contratos de atributos reativos compartilhados devem ser validados por capacidade**: antes de consumir `attr(reactive, "validation_gate")`, checar se os campos esperados existem; em ausencia, aplicar fallback seguro no dado canonico.
- **Quando uma aba precisa expor metadados para outra sem quebrar contrato de retorno**, anexar um atributo reativo opcional (ex.: `review_export_payload`) e manter fallback nulo no consumidor evita breaking change.
- **Modal multi-etapa em Shiny deve ser centralizado em helper unico**: alternar modos por estado (`rv$mode`) reduz duplicacao de observers e evita drift de comportamento entre caminhos de confirmacao/edicao.
- **Web components com nome de tag customizado (ex.: `<lottie-player>`) nao devem ser criados com `shiny::tags$`**: em cenarios com hifen no nome da tag, isso pode disparar `attempt to apply non-function`. Preferir HTML explicito (`shiny::HTML(...)`) ou construcao de tag custom segura.
- **Falha visual nao pode bloquear fluxo funcional**: em eventos de arranque (`start_requested -> run_requested`), envolver `showModal()` em `tryCatch` e manter o disparo do processamento mesmo quando a UI de loading falhar.
- **Em rework de modulo para shell tri-coluna, consolidar outputs por intencao (config/stream/report)** reduz acoplamento entre cards e evita cascata de re-render em layouts com muitos `uiOutput`s fragmentados.
- **Toolbar externa de DataTable em modulo reativo exige eventos delegados com namespace por instancia** para manter busca/paginacao funcionais apos recreacao de DOM por `renderUI`.
- **Fallback de startup em modulo deve ser tipado e observavel**: quando dependencia estatica falha (ex.: `get_dwc_terms()`), retornar estrutura vazia com schema esperado + log explicito evita crash silencioso.
- **`leafletProxy` perde atualizacoes enviadas para um mapa em aba oculta**: outputs tem `suspendWhenHidden = TRUE` por padrao; um `observe` que pinta marcadores via `leafletProxy` enquanto o usuario esta em OUTRA aba tem a mensagem descartada, deixando o mapa desatualizado quando ele volta (regressao ADR-102: coordenada corrigida na aba Coordenadas continuava no mar no mapa da Generalizacao). Fix: `shiny::outputOptions(output, "<map_id>", suspendWhenHidden = FALSE)` para o mapa ficar vivo mesmo escondido. Padrao ja usado em `download_real` (mod_preview) e `file_uploaded` (mod_mapping).
- **`renderUI` que hospeda um `leafletOutput` NAO pode depender de reactives que mudam durante a sessao (dados/correcoes/contagens)**: cada invalidacao re-renderiza o container e **recria o widget leaflet**; o `leafletProxy` seguinte pinta sobre um mapa meio-recriado e o canvas (`preferCanvas`) falha ao repintar o lote maior — sintoma observado (ADR-102): o filtro "Todas" esvaziava o mapa de validacao logo apos aplicar uma correcao. **Regra**: o container do mapa depende SO do resultado-base estavel (ex.: `coord_validation_r()`); TODA atualizacao de marcadores passa por um `observe` + `leafletProxy` separado; elementos condicionais que dependem dos dados (legenda de uma categoria nova) ficam fora do container (ou sempre visiveis), nunca dentro do `renderUI` que carrega o mapa.
- **Observer que sincroniza `input$<x>` -> `reactiveValues` nao distingue "input ainda nao renderizado" de "usuario limpou" so pelo valor atual** (ADR-104, camtrap auto-map). Um `selectInput` devolve `NULL` em dois casos: (a) o card ainda nao foi renderizado/ecoado pelo cliente, (b) o usuario apagou uma selecao que existia. O observer de sync (`input$map_<term>` -> `rv$map_values`) tratava NULL-com-valor-anterior-nao-vazio como (b) e escrevia `""`. Isso apagava valores setados por codigo (auto-map camtrap em extra-terms: `geodeticDatum`, `taxonID`, ...) na janela entre `set_map_value()` e o eco do cliente — base-terms sobreviviam so por ecoar primeiro. **Fix**: rastrear num `new.env()` **nao-reativo** (escrita nele nao re-dispara o observer) quais inputs ja reportaram valor nao-NULL pelo menos uma vez; so interpretar NULL como clear se o input ja foi visto. `set_map_value()` ja escreve `rv$map_values` direto e o `renderUI` le isso reativamente para o `selected`, entao o `updateSelectInput` nem e necessario para o card nascer selecionado — o unico bug era o apagamento. **Regra geral**: qualquer "sync de input para estado" que precise distinguir not-rendered de cleared deve gravar o sinal de "ja renderizou" fora do canal reativo, nunca inferir pelo valor.

## CSS / Bootstrap / bslib

- **`fileInput` gera `input-group`** do Bootstrap. Nao forcar flex externo -- estilizar o `input-group` diretamente.
- **Variaveis CSS referenciadas no codigo devem existir no `:root`**. Gradientes com variaveis indefinidas resultam em UI invisivel (ex: texto branco em fundo transparente).
- **Evitar `!important` como estrategia padrao**: prefira seletores mais especificos e ordem de carregamento correta para sobrescrever estilos do tema `bslib`.
- **Boxes informativos nao devem usar borda grossa unilateral**: evitar padrao de "linha grossa na esquerda"; usar borda completa fina e semantica para `alert`, `notification` e caixas de suporte.
- **Seta de dropdown em pseudo-elemento deve usar escape CSS**: prefira `content: '\25BE'` em vez de caractere literal para evitar glitch de encoding (`â–¾`).
- **Navbar precisa de alinhamento explicito entre nav-links e seletor de idioma**: definir `align-items: center`, altura minima coerente e `gap` entre itens evita desalinhamento horizontal/vertical.
- **`selectInput` no navbar precisa de largura coerente com o padding interno**: manter `width` compativel com `padding-right` e `background-position` evita sobreposicao da seta sobre o texto selecionado.
- **`bslib::page_navbar` pode renderizar links como `ul.navbar-nav > li > a` (sem classes `.nav-item/.nav-link`)**: em regressao de espacamento/padding no header, estilizar tambem o markup `li > a` e nao depender apenas de classes BS5.
- **Seletor de idioma com poucas opcoes no navbar funciona melhor sem selectize**: usar `selectize = FALSE` evita sobreposicao visual da seta e reduz custo de inicializacao.
- **`border-radius` em inputs conectados**: primeiro elemento `X 0 0 X`, segundo `0 X X 0`.
- **`backdrop-filter: blur()`** em modais cria efeito "debaixo d'agua" indesejado. Usar escurecimento simples (`rgba(0,0,0,0.45)`).
- **Scroll container**: usar `scrollbar-gutter: stable` para evitar sobreposicao da barra de rolagem sobre conteudo.
- **`dataTables_length select` pode sobrepor seta e valor** quando herda padding generico de `form-select`; aplicar regra especifica com `padding-right` maior e `background-position` evita colisao visual.
- **Padrao oficial de tabelas do app**: toda `DT::datatable` deve ficar dentro de `.saira-table-shell` para herdar header azul, paginacao compacta e dimensoes consistentes de busca/length menu.
- **Ao alterar `pageLength` por controle externo**, aplicar `table.page.len(len)` seguido de `table.page('first').draw('page')` evita o falso sintoma de "nao mudou para 15" quando a tabela estava em pagina posterior.
- **Quando stats, filtros, mapa e tabela evoluem em ritmos diferentes**, prefira `uiOutput`s separados por painel em vez de um `results_panel` monolitico; isso reduz acoplamento de layout e simplifica manutencao.
- **Full-width por modulo e melhor com override contextual**: usar seletor local (`.tab-content > .tab-pane:has(.validate-coords-page)`) permite remover sobras laterais de uma aba sem quebrar o layout global.
- **Legenda de mapa deve seguir o contrato de dados usado em pills/tabela**: quando filtros usam familias (`validity`, `country`, `sea`, etc.), a legenda precisa usar as mesmas familias para evitar ambiguidade cognitiva.
- **Cores de cluster do Leaflet nao representam tipo de issue**: sempre explicitar em nota de UI quando clustering estiver ativo para evitar leitura errada da legenda.
- **`reference` precisa de explicacao textual na legenda**: sem contexto (capitais/centroides/GBIF/instituicoes), usuarios interpretam `reference` como erro absoluto de coordenada.
- **Mudanca de basemap deve ser rollout controlado**: se trocar tiles por vetor gerar regressao de exibicao de pontos/clusters, voltar rapido ao basemap padrao reduz risco para o usuario final.
- **`fitBounds` deve ser calculado no `leafletProxy` com os pontos filtrados**: usar bbox fixa na inicializacao cria viés regional e piora UX; com pills ativas, o enquadramento precisa refletir o subconjunto atual.
- **Quando a informacao e curta e essencial (onboarding/obrigatorios)**, prefira exibir categorias sempre visiveis em grupos compactos a esconder conteudo atras de tabs clicaveis.
- **Nao depender de `!important` para resolver regressao visual local**: priorizar estrutura de layout (grid/flex), seletores claros e ordem de carregamento previsivel.
- **Texto longo em filho de item de grid/flex estoura o card mesmo com `text-overflow: ellipsis`**: item de grid/flex tem `min-width: auto` por padrao e nao encolhe abaixo da largura intrinseca do conteudo, entao o conteudo `white-space: nowrap` empurra o item para fora da track e a reticencia nunca dispara. Sintoma classico: a previa `.field-card-sample` estourava `.field-card` no grid `.two-column-layout` com `occurrenceRemarks`. Fix estrutural (mesma linha da licao anterior, sem `!important`): `min-width: 0` no item de grid (`.field-card`). Regra de produto associada: previa de card mostra **um** unico exemplo processado em **uma** linha (clip), nao N valores nem multilinha — multilinha deixa os cards altos e poluidos.
- **Tooltip por hover em cards clicaveis precisa de fallback para touch**: em mobile/tablet, exibir descricao inline via media query (`hover: none`/`pointer: coarse`) evita perda de contexto.
- **Em dropzone de superficie inteira, separar copy visual do input nativo**: manter um bloco de texto central com `pointer-events: none` evita captura acidental de clique e simplifica evolucao de layout.
- **Ao mover texto funcional para dentro do ponto de acao, manter hierarquia tipografica do design system**: instrucao em `font-sans` e metadado tecnico (ex.: tamanho maximo) em `font-mono` melhora leitura sem poluir a interface.
- **Se o JS remove markup nativo em runtime, limpar CSS dependente desse markup**: evita regras mortas e estados visuais incoerentes apos refactor.
- **Redesign local de modulo deve usar escopo CSS dedicado**: para telas premium como `help` e `wiki`, prefira classes-raiz (`.help-module`, `.wiki-module`) para impedir regressao transversal em outras abas.
- **Acordeao renderizado por `renderUI` precisa de binding resiliente**: usar event delegation em `document` (`closest('[data-*]')`) evita perder eventos quando o DOM da aba e recriado por busca/idioma.
- **Para acessibilidade em acordeao custom, sincronizar estado visual e ARIA no mesmo ponto**: sempre atualizar `is-open`, `aria-expanded` e `aria-hidden` juntos evita discrepancia entre leitor de tela e animacao.
- **Layout tri-coluna com paineis laterais fixos fica mais estavel com token de offset de header no `:root`** (`calc(100vh - var(--offset))`) em vez de hardcode espalhado por seletor.
- **Quando paineis laterais fixos (240/340) precisarem de mais respiro, priorize full-width do shell antes de mexer nas larguras fixas**: remover `max-width` local e zerar padding lateral do `tab-pane` melhora espaco util sem quebrar contrato visual.
- **Em tabela estreita com 3 colunas no painel fixo, definir largura explicita por coluna e tipografia/celula por intencao reduz o efeito de espremido**: `scientificName` priorizada, `status` compacta e `taxonomicStatus` com quebra controlada.
- **Badges semanticos reutilizaveis (`badge-success/warning/error/info/muted`) simplificam consistencia visual** entre stream e tabela quando ambos representam o mesmo status de dominio.
- **Icone de busca em `textInput` custom so fica estavel com contrato completo de geometria**: use wrapper imediato com `position: relative`, icone absoluto com caixa fixa (`14x14`) e centralizacao real (`top: 50%` + `translateY(-50%)`), e reserve espaco no input com `padding-left` dedicado.
- **No card de busca do Shiny, zere o espacamento herdado antes de ajustar pixel**: definir `margin-bottom: 0` para `.shiny-input-container` e `.control-label` evita deslocamento vertical do icone e elimina o efeito de lupa "fora da caixa".
- **Troca de tipografia global exige tokens, nao hardcodes por modulo**: manter `font-family` literal (ex.: IBM Plex) em blocos locais cria regressao silenciosa; prefira `var(--font-*)` em todo componente reutilizavel.
- **Carregamento de Google Fonts deve ficar no shell da app (`app_ui`)**: evitar `@import` em `custom.css` reduz duplicacao de request e facilita governanca de versao/weights.
- **Adicionar `rel="preconnect"` antes dos `<link>` de CDN reduz latencia de fontes**: inserir preconnect para `fonts.googleapis.com`, `fonts.gstatic.com` (com `crossorigin`) e `cdnjs.cloudflare.com` antes dos stylesheets permite ao browser pre-estabelecer DNS + TCP + TLS antes de precisar dos arquivos, economizando 100-300ms em conexoes lentas.
- **CSS `:has()` e ferramenta valida para substituicao progressiva de conteudo reativo**: `.container:has(.dynamic:not(:empty)) .static { display: none }` esconde o fallback estatico assim que o servidor popula o elemento dinamico, sem JS adicional. Suporte amplo em browsers modernos (Chrome 105+, Firefox 121+, Safari 15.4+).
- **Mono em tamanhos muito pequenos (< 0.85rem) precisa de ajuste fino**: em badges/tabelas compactas, `letter-spacing` leve e `tabular nums` melhoram legibilidade sem alterar layout.
- **Animacao de remocao em listas reativas deve ter fallback de acessibilidade**: sempre adicionar `@media (prefers-reduced-motion: reduce)` para desativar transicoes de `fade/collapse`.
- **Backdrop de modal custom deve usar token de overlay do design system**: centralizar em `var(--overlay)` + blur leve evita divergencia de contraste entre modais.
- **`hidden.bs.modal` dispara no elemento `.modal`, nao em filhos**: ao registrar listener de cleanup em script dentro do modal body, usar `el.closest('.modal')` para subir ao ancestor correto; sem isso, eventos DOM que borbulham para cima nunca alcancam o listener e o cleanup nunca executa.
- **Quando `resolve_review_target()` retorna NULL, feedback ao usuario e obrigatorio**: retorno silencioso sem notificacao viola LESSONS de UX (evitar `req()` silencioso em handlers de acao) e impede diagnostico pelo usuario.
- **Quando uma aba "parece travada" sem footer/paginacao visivel, a primeira hipotese deve ser `bslib::page_navbar(fillable = TRUE)`, nao overflow CSS local**: `fillable = TRUE` e o **default** do `page_navbar` e poe `body { height: 100vh; overflow: hidden }` no shell — abas com conteudo que cresce com interacao do usuario (Wiki paginada com pageLength variavel, Preview com muitas colunas) ficam confinadas ao viewport e o footer da tabela vai parar abaixo da dobra sem scroll. `overflow: hidden` em `.saira-table-shell` ou em outro container interno **nao bloqueia** scroll de pagina — apenas clipa visualmente conteudo que extrapola. Antes de mexer em `overflow`/`!important` em modulos CSS, verifique se a aba precisa de page-scroll opt-in (ADR-085).
- **Page-scroll opt-in por aba via `:has()` e o padrao oficial do projeto**: para abas que precisam liberar `overflow-y: auto` no `.tab-pane` sem afetar o contrato viewport-bound de outras abas, use `.tab-content > .tab-pane:has(.<modulo-class>) { overflow-y: auto; height: auto; max-height: none; }` em `inst/app/www/css/12-overrides.css`. A especificidade do seletor ja vence `.bslib-page-fill` — **nao adicionar `!important`**. O modulo precisa de uma classe marker no container raiz (`wiki-module`, `preview-page`); o seletor combinado em uma unica regra mantem o codigo enxuto (ADR-085).
- **Nao adicionar `scrollY`/`scrollCollapse` ao DT como "fix" para falta de scroll de pagina**: `scrollY` muda o contrato da tabela para "altura fixa com scroll interno vertical e cabecalho fixado durante scroll vertical interno". Misturar isso com falta de page-scroll faz o usuario ter que rolar **dentro** da tabela mesmo quando seleciona pageLength=10 e a tabela caberia na pagina. Se o usuario quer "tabela cresce com pageLength selecionada", o DT default (sem `scrollY`) ja entrega isso — o que falta e page-scroll opt-in (vide licao acima).
- **`!important` nao e ferramenta para sobrescrever layout do bslib/Bootstrap quando especificidade resolve**: antes de adicionar `!important`, calcule se o seletor pode ser mais especifico (ex.: `.tab-content > .tab-pane:has(...)` vence `.bslib-page-fill` por ser mais especifico no ancestor chain). Cada `!important` introduzido conta contra o guardrail `tests/testthat/test-css-guardrails.R` (limite 13). Subir o limite e admissao de derrota e quase sempre evitavel.

## i18n / Internacionalizacao

- **Todo texto visivel** deve passar por `tr(key, lang_r())`. Strings inline do tipo `if (lang == "pt") "X" else "Y"` violam o sistema i18n.
- **Encoding UTF-8** deve ser verificado ao editar `data_dictionary.R`. Caracteres acentuados podem corromper se o editor salvar em Latin-1.
- **Encoding de metadados do pacote e mais seguro com escapes Unicode**: para campos como `Authors@R`/`Description`, `\uXXXX` reduz risco de mojibake entre editores/SO.
- **Placeholder de `selectInput`** nao aceita `uiOutput` -- usar `update*Input()` no server.
- **`DT::datatable` precisa de objeto `language` completo** em apps bilingues: incluir `emptyTable`, `zeroRecords` e `paginate.first/last/next/previous` evita sobras de texto no idioma errado.
- **Ao adicionar novas labels de tabela/filtros em modulo bilingue, atualizar a suite de i18n no mesmo ciclo** previne regressao silenciosa de traducao.

## DwC / Dados de Biodiversidade
- **"Coluna selecionada" nao e "coluna consumida", e derivar uma da outra perde dado** (issue #98). `build_processed_mapping_df()` deixa o valor fixo vencer o mapeamento e da `next` sem ler a coluna, mas `unmapped_raw_columns()` calculava `used_sources` a partir do que estava **selecionado** em `map_values`. A coluna caia entre as duas etapas: nao virava termo DwC (o valor fixo venceu) nem voltava no fim do CSV (contava como usada). O guia de mapeamento tinha uma **terceira** copia da mesma regra e imprimia `Municipio -> country` junto com `="Brasil" -> country`. Regra: quando um passo pode **recusar** uma entrada, quem calcula "o que sobrou" tem que ler a decisao daquele passo, nunca reconstruir a regra por conta propria — aqui, `overridden_mapping_terms()` como fonte unica. Forma irma no mesmo bug: `modified`/`license`/`language` davam `next` **fora** do `if`, entao a coluna mapeada sumia mesmo sem valor fixo nenhum; `datasetName` estava certo desde sempre porque o `next` dele era dentro do `if`. Terceira face da regra: quem **serializa** a precedencia tambem tem que **restaura-la**. O guia gravava o valor fixo como constante, mas o import so restaurava `datasetName`, `license` e `language`, e o termo voltava vazio; e `exclude = names(out)` matava a coluna resgatada de novo quando o nome dela era igual ao do proprio termo.

- **Termo com vocabulario controlado precisa validar a coluna do usuario, nao so decidir precedencia** (2026-08-19). `establishmentMeans` resolvia coluna x assistente com `fill_missing_character_values()`: a coluna vencia onde nao era vazia. Nada checava o valor contra o vocabulario TDWG, entao "Domestico" e "Nativo" iam literais para o CSV, num termo que o GBIF le contra lista fixa. A regra por linha agora e: valor da coluna que JA e o termo controlado vence, depois a resposta do assistente, depois vazio. O que sobra vira alerta na tela de exportacao. Ao mapear termo com vocabulario fechado, pergunte sempre as duas coisas: quem vence, e o valor e valido.

- **Separador de entrada**: `;` (padrao brasileiro). Virgula NAO eh delimitador de tokens.
- **Separador de saida DwC**: ` | ` (pipe com espacos).
- **`scientificName`** eh campo de selecao unica -- nao aceitar multiplas colunas.
- **Validacao taxonomica**: deduplicar `scientificName` antes de consultar e depois propagar resultados por join.
- **Cascata de provedores**: primeiro provedor com match define o veredito; nao combinar provedores simultaneamente para evitar conflito taxonomico.
- **Falha parcial de provedor nao deve abortar cascata**: registrar erro por provedor e continuar com os demais aumenta resiliencia sem mudar contrato do relatorio.
- **`cc_coun` pode ser ruidoso em producao**: em bases com muitos pontos de costa/fronteira, mismatch de pais tende a gerar falso alerta operacional; trate como check opcional quando o foco e triagem rapida.
- **Fluxo principal de coordenadas deve priorizar sinais acionaveis**: para uso de produto, `sea`, `swapped` e `reference` costumam trazer melhor relacao utilidade/ruido.
- **Para checks de consistencia por pais no CoordinateCleaner, `countryCode` e obrigatorio na pratica**: quando planilha nao traz `countryCode`, converter `country` para ISO3 no pipeline evita bloquear o usuario por estrutura de entrada.
- **Conversao de pais precisa de fallback em cadeia, nao de origem unica**: tentar `iso3c`, depois `iso2c`, depois `country.name`, com mapa minimo de aliases comuns, reduz `country_unresolved` em dados heterogeneos.
- **Para aumentar cobertura sem inflar falso positivo, usar cascata em camadas**: `iso3c -> iso2c -> CLDR multilíngue -> aliases -> fuzzy` funciona melhor que pular direto para fuzzy.
- **Aliases de pais devem ficar em artefato versionado (`.rds`) e nao hard-coded em funcao**: isso reduz risco de regressao ao evoluir cobertura e facilita manutencao por script em `data-raw/`.
- **Fuzzy em nomes de pais precisa ser conservador e sempre ultima camada**: exigir tamanho minimo, limite de distancia relativa e melhor match unico evita conversoes erradas em siglas curtas.
- **Derivacao taxonomica**: a partir de `scientificName`, eh possivel derivar `genus`, `specificEpithet` e `taxonRank`. Regra: apenas completar campos vazios/NA, nunca sobrescrever.
- **`eventDate` com 4 colunas**: parser especial (startMonth, startYear, endMonth, endYear) -> `YYYY-MM/YYYY-MM`. Em falha, manter valor bruto sem bloquear.
- **Licencas Creative Commons** tem multiplas formas de URL. Normalizar removendo `http(s)://`, `/legalcode`, e `/` final antes de comparar.
- **NA no CSV exportado**: usar `readr::write_csv(na = "")` para produzir celulas vazias.
- **`basisOfRecord` nao deve usar concatenacao**: tratar como vocabulario controlado de valor unico por linha, com assistente dedicado e saida vazia para valores nao mapeados.
- **Auto-sugestao segura para vocabulario controlado**: limitar a match exato case-insensitive dos termos canonicos reduz falso positivo e acelera mapeamento manual.
- **Home e Preview devem compartilhar a mesma definicao de obrigatorios DwC** para evitar sinais contraditorios ao usuario (onboarding vs prontidao real de export).
- **Quando revisao manual altera `scientificName` no export, o match deve usar normalizacao canonica por `query_name`** (mesmas opcoes da validacao) para propagar correcao em todas as ocorrencias equivalentes.
- **Export NAO carrega colunas de auditoria de revisao manual** (ADR-088 reverte ADR-051/LESSONS antigo). `validacao_manual` e `motivo_revisao` nunca aparecem no `.csv`/`.xlsx` exportados, independente de o usuario ter feito revisao manual de nomes. As correcoes de `scientificName` continuam sendo aplicadas silenciosamente (replacement em `apply_name_review_payload`); rastreabilidade do mapping fica no `mapping_guide.txt` do bundle. Motivacao: usuario achou as duas colunas ruido visual e exigia explicacao extra no upload do IPT a cada export, e a "simplificacao downstream" do design antigo nao compensava.
- **Generalizar coordenada NUNCA aumenta a precisao, mas tambem NUNCA dropa a protecao -- clamp, nao skip** (ADR-105, Chapman 2020). Arredondar para uma grade mais FINA que a precisao que o dado ja tem fabrica precisao: `round("-81.41"/0.001)*0.001 = -81.410` afirma ~110 m num dado de 2 casas (~1.1 km). Chapman e claro: generalizacao **reduz** precisao, nunca a inventa (glossario "precision": 2 casas = 0.01 grau; sec. 4.1 condena fabricar precisao como falsificacao). A 1a tentativa foi **pular** (skip) linhas iguais-ou-mais-finas -- ERRADO: o skip dropava `informationWithheld` + `dataGeneralizations` + scrub de `locality`, expondo um registro sensivel. Chapman sec. 5.1 exige a metadata de sensibilidade record-level **mesmo quando a coordenada nao muda**. Fix correto: `grade_efetiva = pmax(grade_escolhida, precisao_existente)` -- clampa a grade (nunca mais fina), mas **sempre mascara/documenta**. A precisao existente vem das casas decimais (lidas da **string literal** -- `as.numeric` derruba zeros a direita: "-81.40" viraria 1 casa) reconciliada com `coordinatePrecision`. Documentar precisao **provida** (`coordinatePrecision` = grade) E **retida** (texto em `dataGeneralizations`, pois nao ha termo DwC adotado) -- Chapman sec. 4.3/5.1. Lição de teste: ao mudar a grade por linha, varrer fixtures por coords incidentalmente grossas (`-23.5`/`-46.6` com `high`=0.1 virou clamp/no-op e quebrou um teste de justificativa; subir as casas resolve). Regra geral: antes de implementar contra um padrao citado (Chapman/Darwin Core), LER o PDF -- a diferenca entre "skip" e "clamp" so ficou obvia lendo sec. 5.1.

- **`input[[id]]` de `selectInput` devolve NULL quando o usuario LIMPA o campo, nao `""`** (bug encontrado pos-v0.2.2). Observer que sincroniza `input[[map_<term>]]` -> `rv$map_values[[term]]` em `R/mod_mapping.R:620-686` tinha `if (is.null(input_value)) next` para nao sobrescrever map_values na carga inicial (inputs ainda nao renderizados). Mas o mesmo guard fazia o observer **ignorar o clear**, deixando o valor anterior em `rv$map_values` — o termo vazava pro export. Fix: distinguir NULL na carga inicial (old_value tambem vazio em `rv$map_values`, `next`) de NULL apos clear (old_value tinha valor, tratar input como `""` e prosseguir). Padrao geral: sempre que um observer skipa em NULL como otimizacao, validar que a transicao `valor -> NULL` nao e perdida silenciosamente.

- **`camtrapdp::write_dwc()` so exporta linhas que satisfazem `observationLevel == gbifIngestion$observationLevel` (default `"event"`) E `observationType == "animal"`.** Sintetizadores que produzem so linhas media-level (ex.: Wildlife Insights -> Camtrap DP, em `wi_to_camtrap_csv()`) precisam declarar o nivel explicitamente: `pkg$gbifIngestion$observationLevel <- "media"` apos `read_camtrapdp()`. Sem isso, o filtro elimina tudo silenciosamente, `write_dwc()` retorna um data.frame com colunas certas mas `nrow == 0`, e o pipeline downstream (Preview, Validacao, Export no Saira) entra em estado "no_data". Sintoma classico: aba de Mapeamento mostra as 41 colunas (`names()` funciona em df vazio), mas Preview fica em branco e botoes de Validar nao habilitam. Corolario: ao sintetizar `observations.csv` para o Camtrap DP, IDs precisam respeitar a spec — `mediaID` unico em `media.csv`, `observationID` unico em `observations.csv`. WI permite multiplas identificacoes por imagem (~0.3% no dataset real), entao `observationID = image_id` colide; suffixar `<image_id>-obs-<seq>` e deduplicar `media.csv` por `image_id` resolve sem perder dado. Guardrail recomendado: `convert_camtrap_to_dwc_occurrence()` deve falhar com erro i18n claro quando `nrow(occ) == 0`, em vez de propagar dataframe vazio adiante.
- **`camtrapdp::write_dwc()` emite um SCHEMA FIXO de Occurrence (~41 colunas); termos sem dado na fonte voltam como colunas inteiramente vazias** (ADR-104). No `example_dataset()`, `organismID`, `minimum/maximumDepthInMeters` e `identificationVerificationStatus` saem 100% NA. Repassadas adiante viram cards de mapeamento vazios e colunas de preview so-com-titulo — o usuario ate mapeia uma a mao e o resultado e vazio. Fix: `convert_camtrap_to_dwc_occurrence()` descarta toda coluna all-blank (`any(!is.na(v) & nzchar(trimws(v)))`) antes de devolver o df. Regra geral: ao consumir um conversor de schema-fixo (write_dwc, qualquer crosswalk DwC), nunca assumir que "coluna presente == coluna com dado"; filtrar vazias na borda da ingestao.
- **Colunas de camtrap JA SAO termos DwC canonicos — mapeie por identidade, nao pelo motor fuzzy** (ADR-104). Rodar o Rostrum (scoring fuzzy + ambiguidade) em dado onde a resposta e trivial (coluna `X` -> termo `X`) produz dois defeitos: (1) rebaixa exact-matches de extra-terms para SUGERIDO (a regra "extra-term nao ganha AUTO" existe para uploads genericos onde o nome coincidente nao garante intencao — mas write_dwc GARANTE que sao DwC reais); (2) inventa cross-matches espurios marcados applied (visto: coluna `decimalLatitude` sugerida para termo `verbatimLongitude`). Fix: `perform_auto_map()` despacha para um `perform_camtrap_automap()` deterministico quando `attr(raw_data_r(), "saira_camtrap_source")` existe — cada coluna-que-e-termo vira AUTO direto. Lição: quando a fonte garante que o nome da coluna == termo destino, pule o matcher heuristico; ele so adiciona ruido e baixa confianca a um mapeamento que ja e certo.
- **Chamada de rede no boundary de export tem que degradar para `NA`, nunca bloquear nem quebrar o export** (ADR-107). O enriquecimento de status IUCN (`fetch_gbif_iucn_category`, primeiro request externo do app) e estritamente opcional: `httr2` em Suggests (`requireNamespace` gate), `req_timeout` curto, `tryCatch` total em volta de TODO o caminho HTTP, e qualquer falha (pacote ausente, offline, non-200, payload inesperado) vira `NA` — a chave simplesmente nao e escrita. Duas camadas de defesa: (1) o helper de rede trapa por dentro; (2) `apply_conservation_status` envolve **cada fonte separadamente** num `tryCatch`, entao uma falha de IUCN nunca derruba as chaves MMA locais. Memo por sessao (`create_rds_cache`) evita refazer o mesmo request. Regra geral: I/O externo num pipeline de geracao de arquivo nunca pode ser o motivo de o arquivo nao sair; trate-o como best-effort com fallback silencioso e teste explicitamente o caminho "sem rede".

- **Parser com whitelist de regex nao falha: ele deixa passar** (datas com `/` no export, 2026-08-20). `parse_dates_to_iso()` cobria `DD/MM/YYYY` e `YYYY-MM-DD`, e `fix_dates_to_iso()` mantinha o valor bruto no que nao casasse -- entao `2023/12/25`, `25/12/2023 14:30` e `12/2023` chegavam ao ZIP com o separador da planilha, sem erro nem aviso em lugar nenhum. O fallback "mantem o bruto" e correto (um intervalo `YYYY-MM/YYYY-MM` nao pode ser reescrito), mas esconde o buraco de cobertura. Ao adicionar formato: separar a hora ANTES do voto day-first (a regex ancorada em `$` nao ve `25/12/2023 14:30`, e a coluna inteira perde o voto), e reatachar a hora com `T` em vez de descarta-la. Corolario que motivou a mesma PR: **formato valido nao e valor plausivel** -- `2098-05-01` passa por todo o pipeline porque a data e bem-formada; so uma checagem de faixa de ano (`date_year_issues()`, 1600..ano atual, convencao GBIF) pega o erro de digitacao, e ela avisa sem bloquear porque registro historico legitimo existe.

## Performance

- **`sapply()` element-wise** eh lento com 99k+ linhas. Vetorizar parsing de datas testando cada formato em lote.
- **`as.Date(..., format=...)` sem mascara previa** pode aceitar entradas parcialmente e gerar anos incorretos (ex: `0023`). Usar regex estrita por formato antes de parsear.
- **Ano com 2 digitos (`DD/MM/YY`)** precisa de regra de seculo explicita. No Saira: cutoff dinamico (`YY <= ano atual (2d) -> 20YY`, senao `19YY`).
- **RDS estatico** (como `dwc_terms.rds`) nao deve ser relido do disco a cada mudanca reativa. Cachear na primeira leitura.
- **Quando houver artefatos estaticos correlatos** (ex.: termos e sinonimos), aplicar o mesmo padrao de cache para manter consistencia arquitetural e evitar "ilhas" de I/O repetido.
- **Preview de assistente em dataset grande**: limitar mapeamento ao subconjunto exibido (ex.: primeiras 5 linhas) e usar lookup vetorizado para contagens globais evita recomputacao pesada a cada interacao de UI.
- **`vapply(x, f, ...)` com funcao escalar em 96k+ elementos** eh ordens de magnitude mais lento que equivalente vetorizado. `trimws()`, `tolower()`, `%in%` sao nativamente vetorizados em R e operam em C; criar versoes vetorizadas de helpers (`normalize_keys`, `sanitize_terms`) em vez de aplicar a versao escalar via `vapply`/`for`.
- **`head(..., 100)` deve acontecer antes do processamento caro**: se o subset for aplicado depois do pipeline completo, o custo total permanece O(n) e o preview continua lento.
- **Evitar geracao de UUID em massa no caminho de preview**: IDs completos para todo dataset devem ser gerados apenas no fluxo completo; no preview, IDs leves para 100 linhas sao suficientes.
- **Reutilizar a mesma funcao pura com datasets diferentes (subset vs completo)**: aplicar `build_processed_mapping_df(...)` tanto no preview leve quanto no fluxo completo evita divergencia funcional e mantem manutencao simples.
- **Consultas taxonomicas**: sempre consultar `taxadb` em lote e sobre nomes unicos para reduzir custo e I/O de banco local.
- **Separar "prontidao" de "processamento" na validacao taxonomica**: ler apenas estado de mapeamento para habilitar/desabilitar botao e deixar `prepare_taxadb_inputs()`/taxadb para o clique explicito reduz recalculo O(n) em navegacao.
- **CoordinateCleaner em volume alto exige poda de candidatos**: executar checks espaciais apenas em linhas com coordenadas formalmente validas reduz custo sem perder cobertura relevante.
- **Perfis de execucao (`complete`/`fast`) ajudam a manter UX previsivel**: para bases grandes, oferecer subset de checks evita travamento percebido e permite diagnostico incremental.
- **Preservar cardinalidade e crucial em validacao de qualidade**: pipeline deve sempre manter `nrow(out) == nrow(in)` e apenas marcar diagnosticos, sem excluir linhas do usuario.
- **Resolucao de pais em vetor grande deve operar sobre valores unicos e re-expandir depois**: `unique -> resolver em lote -> map de volta` reduz custo sem mudar semantica.
- **Caches separados para aliases e referencia fuzzy evitam reconstrucoes caras por chamada**: ler `country_aliases.rds` e montar codelist apenas uma vez por sessao melhora throughput do pipeline.
- **Resolucao de lookup sobre nomes unicos e re-expansao por indice e o padrao de performance para tabelas de relatorio em DT**: em `output$renderDataTable` com `N` linhas e `k << N` nomes unicos, chamar `f(sensitive_source)` (O(N)) em vez de `f(unique(sensitive_source))` seguido de `match()` (O(k)) causa regressao de ~30x em datasets reais (benchmark: 5000 linhas / 35 nomes unicos: 2.1s vs 0.07s). Aplicar sempre que o resultado de um lookup depende apenas do *valor* do campo, nao da posicao da linha. Padrao: `u <- unique(x); result_u <- f(u); result_x <- result_u[match(x, u)]`. Ver `output$report_table` em `mod_validate_names.R` (corrigido 2026-05-19). O mesmo padrao ja estava correto em `sensitive_overview` em `mod_preview.R` ("resolved over unique names so a large dataset is not normalized row by row") — usar como referencia.
- **`leafletProxy` em loop por linha trava o app em datasets grandes; lotear + deduplicar**: pintar N feicoes chamando `addRectangles`/`addCircles`/`addPolylines` UMA VEZ por linha gera N mensagens WebSocket por tipo de feicao (no overlay da Generalizacao eram 3N: conector + celula + circulo), e com `suspendWhenHidden = FALSE` isso roda mesmo na aba oculta — marcar uma especie sensivel na aba de Nomes/Coords congelava tudo (camera-trap: milhares de registros). Fix: (1) as funcoes `add*` do leaflet sao **vetorizadas** — passar vetores de coordenadas/cor/popup e desenhar tudo em UMA chamada; para varios segmentos separados em `addPolylines`, intercalar `NA` entre pares (`as.vector(rbind(x1, x2, NA))`) que o leaflet trata como quebra; (2) **deduplicar por assinatura geometrica** antes de desenhar (`!duplicated(paste(...))`) — dados de armadilha fotografica empilham milhares de registros nas mesmas coordenadas de camera, entao a celula/marcador identicos sao desenhados uma so vez (tambem elimina o artefato de opacidade composta de retangulos translucidos sobrepostos). De 3N+N chamadas para ~4 chamadas em lote. Ver overlay em `mod_sensitive_coords.R` (corrigido 2026-06-14).
- **O custo da Generalizacao estava no COMPUTO por registro, nao no desenho**: `generalization_map_preview()` chamava `sensitive_resolve()` (construcao de chave + match MMA) e o lookup de grade por tier **a cada linha** (O(n) sobre milhares de nomes/tiers identicos), alem da busca espacial de fronteira (`terra::extract`) ponto a ponto — e tudo isso rodava DUAS vezes por resposta (card de resultado + mapa). Fix: resolver sobre **nomes/tiers/coordenadas unicos** e re-expandir por `match()` (≈2,5 s → ≈0,2 s em 6k registros). `sensitive_resolve()` agora deduplica internamente (recursa sobre `unique(names)`), entao todo chamador se beneficia. Lição geral: antes de otimizar render/Leaflet, perfilar — o gargalo costuma ser uma funcao pura O(n) chamada por linha, nao o desenho. Complementar: **debounce** (`shiny::debounce(reactive, 300)`) no overlay do mapa tira o caminho pesado do flush da resposta, deixando pills/badges instantaneos sem modal bloqueante. Ver `generalization_map_preview` em `utils_sensitive.R` e `gen_overlay_data_r` em `mod_sensitive_coords.R` (corrigido 2026-06-15).
- **`trimws()`/`strsplit()` por celula em pipeline de mapeamento e O(n) escondido e caro**: `normalize_semicolon_tokens` aplicava `split_semicolon_tokens` (strsplit + `trimws`) em CADA celula de CADA coluna mapeada, mesmo celulas de valor unico sem `;`. `trimws()` base roda `match.arg` + um `sub` regex por chamada, entao em 8,5k linhas × 25 colunas dava ~15 s por recomputo de `build_processed_mapping_df` (e o overlay sempre-ativo da Generalizacao fazia uma simples edicao de mapeamento dispara-lo). Fix: vetorizar o caminho comum — um `trimws()` vetorizado resolve as celulas sem separador (a maioria esmagadora) e so as que contem `;` (`grepl(";", x, fixed=TRUE)`) entram no caminho por celula; mais um atalho para coluna-fonte unica que pula o re-join por linha em `collapse_mapped_values`. ~15 s → ~0,4 s. Regra: nunca aplicar `trimws`/`strsplit`/regex por celula via `vapply`/`for` quando der para rodar a versao vetorizada na coluna inteira e tratar so o subconjunto que realmente precisa. Ver `utils_mapping.R` (corrigido 2026-06-15).
- **`extract_scientific_name_components` parseava binomio POR LINHA**: derivar genus/specificEpithet/taxonRank de `scientificName` via `lapply` por linha (com `gsub`/`sub`/`trimws` em cada uma) era o segundo maior custo de `build_processed_mapping_df` — ~2,4 s em 8,5k linhas, embora um dataset tenha um punhado de especies unicas (camtrap: ~6). Fix: recursa sobre `unique(names)` e re-expande por `match()` (~2,4 s → ~0,2 s), comportamento identico. Diagnostico importante: o lag de 6-7s ao mapear um campo num dataset camtrap NAO era render nem a aba de Generalizacao — era este pipeline puro recomputando a cada edicao (qualquer consumidor de `mapped_data` o forca). Sintoma camtrap-especifico apenas porque o auto-map preenche ~40 colunas × milhares de linhas; um CSV pequeno mal aparecia. Lição: quando "X e lento so com dataset grande/camtrap", perfilar a funcao pura O(n) primeiro — quase sempre e dedup-de-valores-unicos, nao reatividade. Complementar de UI: `output$mapping_ui` (renderUI) recomputa a `processed_preview_for_term` (amostra head(200)) de TODOS os cards mapeados a cada edicao; memoizar por (term, colunas, n) num `new.env` dataset-scoped faz so o card editado recomputar. Ver `mod_mapping.R` (corrigido 2026-06-15).
- **`tr()` dentro de laco por linha era o maior multiplicador do app** (ADR-111). Duas causas somadas: (a) `resolve_i18n_dict()` fazia `exists("i18n_dict", inherits = TRUE)` e `exists(envir = asNamespace("saira"))` a **cada** chamada, procurando um objeto que nao existe desde a migracao do dicionario para JSON, e (b) `!key %in% names(dict)` materializava um vetor de 869 nomes por chamada. Ler o cache quente primeiro e trocar por `entry <- dict[[key]]; is.null(entry)` deu 5,5x (1,59 s -> 0,29 s em 50k chamadas). Regra geral: **`names(x)` dentro de funcao chamada em laco e alocacao escondida**; em lista, `x[[chave]]` ja devolve `NULL` para chave ausente e dispensa o teste de pertinencia. Cuidado: em **vetor nomeado** o mesmo `[[chave]]` **erra** (ver ADR-110).
- **Ler um reativo (`lang_r()`) dentro de um laco por linha custa mais que a funcao que ele alimenta**: `mod_validate_coords.R` chamava `tr(chave, lang_r())` uma vez por registro na montagem dos badges. Resolver o mapa sobre `unique()` corta as duas coisas de uma vez (1,92 s -> 0,005 s em 50k linhas).
- **Harness de benchmark: `for (i in 1:n) force(expr)` mede UMA chamada, nao `n`.** `expr` chega como promise; `force()` avalia na primeira iteracao e as seguintes so leem o valor ja em cache. Isso fez `get_active_dwc_terms_list()` parecer custar 0,001 s por 200 chamadas quando custava 0,19 s, e quase levou a descartar uma otimizacao real de 3x por "falta de margem". Usar `e <- substitute(expr); p <- parent.frame(); for (i in seq_len(n)) eval(e, p)`, que reavalia a cada iteracao; o overhead do `eval()` e identico nos dois lados da comparacao.
- **Nem toda dependencia reativa "desperdicada" e desperdicio: ela pode ser o que mantem um observer em passo com a re-renderizacao que ela mesma dispara** (ADR-111, reprovado em teste manual). O observer de sincronia do mapeamento lia `lang_r()` por tabela, e trocar de idioma reexecutava a varredura dos 66 termos sem que nenhum valor de mapeamento dependa de idioma — parecia otimizacao obvia isolar. Mas `output$mapping_ui` **tambem** depende de `lang_r()`: a troca destroi e recria todos os `map_<termo>`. A dependencia fazia o observer re-rodar no **mesmo flush**, reafirmando `rv$map_values` dos inputs ainda validos antes da grade ser remontada. Isolada, o observer so acordava apos o rebind do cliente, lia `NULL` com `rendered_map_inputs` ja `TRUE` e caia no ramo "usuario limpou" (ADR-104), apagando o mapeamento inteiro. Regra: **antes de isolar uma leitura num observer, verificar se algum `renderUI` depende da mesma fonte e recria os inputs que esse observer le** — se sim, a dependencia e o sincronismo, nao o custo.
- **`testServer` nao simula unbind/rebind de input no DOM**, entao regressao causada por re-render de `renderUI` que recria inputs passa verde se o teste ler `input$x` (o registro do servidor guarda o valor antigo). **Mas o HTML re-renderizado esta ao alcance**: `output$<id>$html` carrega o `checked`/`value`/`selected` que o cliente vai ecoar de volta, e afirmar sobre ele detecta a classe inteira. Ver o teste da issue #110 em `test-mod-sensitive-coords-server.R`. Corrige a versao anterior desta licao, que dava o gate manual como unico detector.
- **Contagem por categoria com `vapply(categorias, function(c) sum(vec == c))` e O(categorias x linhas)**: uma passada de `table(vec[filtro])` indexada por nome resolve em O(linhas). Alem do ganho, `table()` descarta `NA` naturalmente, evitando o `sum(NA & TRUE) -> NA` que envenenava a contagem e quebrava um `if (!any(ok))` a jusante (`mod_sensitive_coords.R`, corrigido em ADR-111).
- **Ler `lang_r()` dentro de um `reactive()` que LE DO DISCO transforma troca de idioma em novo upload** (ADR-112). `raw_data` em `mod_upload.R` lia o idioma em dez pontos, todos so para escolher o texto de uma notificacao, e isso fazia o reativo inteiro depender do idioma: trocar PT/EN relia o arquivo e emitia um data frame novo, que o `observeEvent(raw_data_r())` do mapeamento corretamente tratava como upload novo, apagando mapeamento, os dois assistentes e os caches. Regra: **idioma so pode ser dependencia reativa de quem produz TEXTO**; em qualquer reativo que produz DADO, ler sob `isolate()`. Notificacao e evento pontual, deve sair na lingua do momento em que ocorreu, nunca ser reemitida.
- **Sonda generica para dependencia reativa indevida: trocar o conteudo do arquivo no disco entre duas leituras.** Reativo cacheado devolve a contagem antiga; reativo que reexecutou pega a nova. `identical()` sobre os dois retornos **nao** serve — reler o mesmo arquivo produz objeto igual e o teste passa verde com o bug dentro. Sempre parear com um teste de que a invalidacao legitima (arquivo novo) ainda funciona, senao o teste vira "nunca rele".
- **Alocar uma tabela de lookup DENTRO de funcao chamada por linha e a mesma classe de alocacao escondida que `names(x)` em laco** (ADR-113). `parse_month_to_number` reconstruia um vetor nomeado de 40 entradas a **cada** chamada, o que sozinho fazia dela a pior das quatro (5,85s em 50k linhas contra 4,05s da de ano, que so faz regex). Hoistar para constante de arquivo lida **tanto pelo escalar quanto pelo companion vetorizado** resolve o custo e impede que as duas grafias da tabela divirjam com o tempo. Regra: constante literal dentro de corpo de funcao e alocacao por chamada, nao "custo zero de leitura".
- **`regmatches(x, regexpr(...))` DESCARTA elementos sem match e encurta o vetor** (ADR-113). Ao vetorizar um extrator escalar de regex isso desalinha silenciosamente tudo depois do primeiro miss, e o teste so pega se a fixture tiver um valor sem match no meio (nao no fim). Padrao seguro: `m <- regexpr(p, x, perl = TRUE); ok <- m > 0L; out[ok] <- substring(x[ok], m[ok], m[ok] + k)`. Mesma familia da armadilha de `[[chave]]` em vetor nomeado (ADR-110): funcao base que parece elemento-a-elemento mas muda o comprimento.
- **Ao vetorizar um helper escalar, o ramo que precisa de atencao e o que NAO retorna cedo** (ADR-113). `parse_month_to_number` reconhece `^\d{1,2}$` mas so retorna se o numero estiver em 1..12; `"0"` e `"13"` **atravessam** para a busca por nome (onde erram e viram `NA`). Uma vetorizacao ingenua que devolve `NA` no passo numerico concorda no resultado de hoje e diverge no dia em que um nome de mes parecer numerico. Ao transliterar escalar -> vetorizado, mapear cada `return()` **e cada nao-return**, e transformar os fall-throughs em casos de teste nomeados.
- **`vapply` sobre vetor de caracteres devolve vetor NOMEADO (`USE.NAMES = TRUE` e o default), e esses nomes vazam para tudo que se deriva dele** (ADR-113). Ao trocar quatro `vapply` por companions vetorizados, `valid_mask`, `partial_mask` e `failed_rows` -- todos retornados em listas publicas -- perderam um atributo `names` que ninguem pedira nem lia. Valores identicos por digest. Antes de tratar isso como quebra de contrato ou de "consertar" recriando os nomes (que e readicionar o desperdicio recem-removido), procurar consumidor real: aqui o unico acesso em teste era posicional (`out$failed_rows[[3]]`).
- **Ler um campo de `reactiveValues` SO para testar se e `NULL` cria a mesma dependencia que le-lo pelo valor** (ADR-113). `reactiveValues` invalida na escrita independentemente de o valor ter mudado. Em `mod_validate_names.R` a linha `show_progress_meta <- is_busy || !is.null(rv$run_state) || has_report` era atribuida e **nunca lida**, e sozinha bastava para o painel inteiro de configuracao (incluindo dois `input_switch` recriados) ser reconstruido ~16x/s durante uma execucao. Extrair o bloco de progresso para output proprio **sem** apagar essa linha nao produz efeito nenhum, e **nenhum teste automatizado distingue as duas versoes**. Ao dividir um `renderUI` para soltar uma dependencia, varrer o corpo **inteiro** atras de toda leitura da fonte, inclusive as cujo resultado e descartado -- e conferir se locais vizinhos (aqui `progress_snapshot()` e `validation_result()`) so existiam para alimentar a linha morta.
- **Truncar depois de ordenar materializa o frame inteiro; ordenar um indice e truncar o indice, nao** (ADR-113). `stream_window()` fazia `out[order(...), , drop = FALSE]` sobre todo o stream acumulado e so entao ficava com 100 linhas, a cada tick de 60ms de uma execucao. Ordenar para indice, cortar o indice e subconjuntar uma vez da saida identica: 1,02s -> 0,06s em 200 redraws sobre 20k nomes, e a distancia cresce com o tamanho do stream porque o custo do subset passa a ser fixo.
- **`pkgload::load_all()` carrega TODAS as dependencias declaradas em `Imports`, o R nao** (ADR-113). Pacote so acessado com `::` e preguicoso sob `loadNamespace()`, mas o pkgload o carrega de qualquer forma -- `taxadb`, `writexl` e `leaflet` aparecem em `loadedNamespaces()` apos `load_all()` sem ninguem os ter chamado. Consequencia pratica: **medir custo de carga do pacote sob `load_all()` da numero errado e esconde o ganho**. Para medir de verdade, `R CMD INSTALL` numa lib temporaria e cronometrar `loadNamespace()`, que e como o app implantado carrega. Foi a diferenca entre "nao mudou nada" (9,5s -> 8,9s sob load_all) e o ganho real (5,78s -> 1,04s instalado).
- **Antes de otimizar, rodar o suite de perf na `main`: ele pode ja estar vermelho** (ADR-113). `RUN_PERF=true` na `main` acusava 3 falhas (Stage 2 a 7,7s contra teto de 2s; pipeline completo a 10,5s contra 8s; Stage 3 a 1,6s contra 0,5s). Sem essa linha de base, as duas primeiras seriam creditadas erradamente como "regressao introduzida pela PR" ou, pior, o verde parcial depois da correcao seria lido como falha. Um budget na fronteira tambem precisa de mediana de varias execucoes antes de ser chamado de regressao: o de `dynamicProperties` oscila em torno dos 0,5s nesta maquina identicamente nos dois lados (0,478s contra 0,486s).

- **Squash-merge com `--delete-branch` numa PR empilhada FECHA a PR filha, e ela nao pode ser reaberta nem reapontada** (ocorrido no lote da ADR-113). A #75 saia de `perf/vectorize-mapping-parsers` (a #74) porque as duas mexiam na mesma funcao e no mesmo arquivo de budgets; ao mergear a #74 com `--delete-branch`, o GitHub apagou a base da #75 e a fechou automaticamente. `gh pr edit --base main` e o PATCH da REST API falham os dois com "Cannot change the base branch of a closed pull request", e `gh pr reopen` falha com "Could not open the pull request". Nada se perde (a branch head sobrevive e os commits estao la), mas a PR e a discussao dela ficam orfas e e preciso abrir uma nova apontando para a antiga. Ordem correta: **reapontar a filha para `main` ANTES de mergear a mae**, ou mergear a mae sem `--delete-branch` e so limpar a branch depois que a filha entrar.
- **Varias PRs de um mesmo lote colidem no `CHANGELOG.md` mesmo sem tocar no mesmo codigo**: todas escrevem na secao `[Unreleased]`, entao a segunda em diante conflita sempre. Resolver mergeando `main` na branch (nao rebase com force-push, proibido em branch publicada) e **manter os dois lados** — o conflito e aditivo, nao concorrente. Vale mergear as PRs em sequencia e so resolver o conflito uma vez por PR, depois que a anterior entrou; resolver antes significa resolver de novo.
- **Ao resolver conflito concatenando os dois lados, conferir que o resultado ainda PARSEIA antes de rodar a suite.** No lote da ADR-113 a regiao de conflito no arquivo de budgets terminava *antes* do `})` compartilhado dos dois blocos, entao juntar `theirs + ours` deixou o bloco vindo do `main` sem fechar. `git` nao valida sintaxe; o sintoma foi um erro de parse cru na suite inteira, sem apontar a causa. Cheque barato antes de commitar: `Rscript -e 'parse("arquivo.R")'` e contagem de chaves/parenteses.


## Testes

- **`testthat.R`** na raiz de `tests/` eh o runner padrao do pacote.
- **Testes de regressao**: ao corrigir bug de badges/estado reativo, adicionar teste que alterna filtros e verifica estabilidade.
- **Funcoes puras em `utils_*.R`** devem ser testaveis sem carregar Shiny. Extrair logica dos modulos para utilities.
- **Extracao de reactive para funcao pura**: para preservar contrato e observabilidade, retornar lista composta com `data` e contadores auxiliares (ex.: `eventdate_failure_count`) e sincronizar estado reativo no modulo.
- **Caches globais exigem reset explicito em teste**: usar setup/teardown com helper interno de reset para eliminar dependencia de ordem entre casos.
- **Quando a suite ja existe, expandir cobertura em vez de recriar do zero**: reduz churn e preserva historico de regressao.
- **Para testes de I/O, usar fixtures temporarias deterministicas** (incluindo BOM UTF-8 e delimitadores em empate) para garantir reproducibilidade sem dependencias externas.
- **Cobrir funcoes de pipeline e funcoes auxiliares separadamente** (`process_for_export` + helpers), para localizar regressao com menor custo de diagnostico.
- **Para modulos Shiny, validar contrato de retorno com `session$getReturned()`** em `testServer` reduz acoplamento com detalhes internos de `output$*`.
- **Testar gatilho de custo pesado, nao apenas resultado final**: alem de conferir dataframe final, validar se o caminho pesado roda apenas em acoes explicitas (ex.: download/validar) previne regressao de performance.
- **Mockar wrappers do motor espacial e melhor que mockar pacote externo inteiro**: em testes unitarios de coordenadas, stubs locais para `cc_*` e resolucao de pais deixam os cenarios deterministicos e rapidos.
- **Default de modulo que le o cache em disco (`tools::R_user_dir`) no init torna o teste dependente da maquina**: quando `mod_validate_names_server` passou a pre-selecionar provedores BR ja baixados (default = `c("gbif", baixados)`), testes que assumiam `selected_providers == "gbif"` ou contavam batches da cascata por `rv$selected_providers` quebravam SO na maquina do dev (que tinha florabr/faunabr baixados) e passavam no CI (cache limpo). Fix: `testthat::local_mocked_bindings(brprovider_data_available = function(provider_id) FALSE, .package = "saira")` no topo de cada teste que inicia o modulo e depende do conjunto de provedores — fixa "nada baixado" deterministicamente. Para cobrir o caminho novo, um teste dedicado mocka o retorno `TRUE` para um provedor e afirma o pre-select.
- **Cobrir prioridade de diagnostico como regra explicita**: testes devem verificar que cada linha recebe um unico diagnostico final, mesmo quando varias flags estao ativas.
- **Em modulo com filtro padrao pos-run, validar primeiro o estado default e depois os cliques de pills**: isso evita falso negativo de teste por expectativa desalinhada com UX real.
- **Cobrir contrato com canal principal + canal leve auxiliar**: quando modulo retorna reactive completo e expoe preview leve por atributo interno, testes devem verificar ambos os contratos para garantir wiring correto no `app_server`.
- **Ao combinar resultados de provedores**: alinhar colunas antes de `rbind` para evitar erro quando resultados possuem schemas diferentes.
- **`data.frame` com vetores nomeados**: definir `row.names = NULL` evita erro de rownames NA quando o vetor carrega nomes inesperados.
- **Testar caminho leve sem tocar no caminho pesado**: usar contador de chamadas no `mapped_data_r()` em `testServer` confirma que `quick_inputs()`/`can_run_validation()` nao materializam dados completos quando o gate leve esta presente.
- **Para UX de acoes longas, testar transicao de estado e nao so resultado final**: validar estados intermediarios (`starting`, `running`) reduz regressao de feedback visual em cliques.
- **Em testes de resolucao de pais, cobrir casos adversariais de fuzzy e obrigatorio**: alem dos matches esperados, validar entradas sem solucao (`xyzxyz`) e ambiguas para evitar falso positivo silencioso.
- **Benchmark funcional deve acompanhar mudancas em fallback de pais**: manter budget explicito (ex.: 10k linhas < 1s em ambiente local) ajuda a detectar regressao de performance cedo.
- **Ao endurecer parser de CSV, cobrir BOM + arquivo vazio no mesmo ciclo**: isso evita regressao em `detect_delimiter()` para entradas exportadas por Notepad/Excel.
- **Para hardening de startup, adicionar teste de modulo com dependencia mockada em erro**: validar que o modulo sobe mesmo com falha externa previne regressao de disponibilidade.

## Package Check / Deploy

- Em pacote R, evitar `source()` dentro de arquivos em `R/`; carregar funcoes via namespace previne divergencia entre ambiente dev e tarball.
- Em testes executados por `R CMD check`, nao depender de caminho local para `R/*.R`; preferir `getFromNamespace()` para funcoes internas.
- Para arquivos de teste (ex.: RDS), preferir `system.file(..., package = "saira")` com fallback de desenvolvimento quando necessario.
- Ao adicionar chamadas `pkg::fun()`, garantir dependencia declarada em `Imports` para evitar warning de dependencia nao declarada.
- Ao introduzir `leaflet` em modulo Shiny, declarar `leaflet` em `DESCRIPTION::Imports`; uso apenas com `leaflet::` nao elimina a necessidade no check do pacote.
- Dependencias espaciais do CoordinateCleaner em fluxo principal devem ser declaradas explicitamente em `Imports` (`CoordinateCleaner`, `countrycode`, `sf`, `rnaturalearth`, `rnaturalearthdata`) para reduzir drift entre ambientes.
- Para remover warning de portabilidade por non-ASCII em `R/*.R`, usar escapes Unicode (`\\uXXXX`) preserva o valor das strings em runtime sem mudar API.
- Quando criar scripts auxiliares em `data-raw/`, adicionar `^data-raw$` em `.Rbuildignore` evita levar material de geracao para o build final do pacote.
- Quando o app usa `addResourcePath("www", ...)`, assets internos devem manter prefixo `www/` nas URLs de runtime (`www/lottie/...`, `www/custom.css`, etc.). Trocar para caminho sem prefixo pode gerar 404 em runtime.
- **Run de CI lento com cache hit nao e o R, e o apt** (2026-08-19). Causa exata, achada no log do `setup-r@v2`: o runner resolve apt por mirrorlist com `azure.archive.ubuntu.com` na frente e `archive.ubuntu.com` atras. Com o mirror da frente fora do ar, o apt gasta timeout cheio mais tres retries em CADA indice e CADA pacote antes de cair para a reserva (`Ign:` repetido quatro vezes, depois `Hit:` na reserva). Os "33 kB/s" do log sao a media incluindo espera morta, nao a banda real. Limitar `Acquire::*::Timeout` e `Acquire::Retries` torna o failover imediato e nao muda nada em dia bom. Um run de 72 min registrou `Cache hit` e `Cache restored successfully`, zero pacote R compilado, e `R CMD check` em 3m36s. Os 58 min restantes sairam de uma linha: `Fetched 117 MB in 58min 22s (33.4 kB/s)`. `setup-r-dependencies` instala os *system requirements* (`libgdal-dev`, `libboost`, `libhdf5-dev`, `cmake`) com apt a cada execucao, e o `actions/cache` cobre so a biblioteca R, nunca essa camada. Ela e invisivel enquanto o mirror Ubuntu do runner responde rapido. Antes de mexer em cache ou dependencia, procure a linha `Fetched` no log e compare com o tempo do passo.

## Upload / Dropzone (2026-02-23)

- **Separar responsabilidade visual evita regressao recorrente**: tratar dropzone (`upload-dropzone`) e progresso nativo (`upload-native-input`) como blocos distintos simplifica manutencao e impede que a barra "vaze" para dentro da area de arrastar/soltar.
- **No `fileInput` do Shiny, remover apenas o shell visual (`.input-group`) e mais seguro que substituir o componente inteiro**: manter o `input[type=file]` real preserva binding, upload e lifecycle nativo.
- **Se o JS faz detach de markup nativo, o seletor de busca do input precisa cobrir os dois contextos (dropzone e container irmao)** para evitar quebra quando o DOM muda entre iteracoes.
- **A barra de progresso do Shiny recebe estilo inline no elemento `#<id>_progress`**: para largura realmente integral, usar seletor especifico com `width/min-width/max-width` e `!important` no escopo local.
- **Container de progress com `border/background` vira "chip involuntario"**: para mostrar apenas a barra verde, deixar o container transparente e aplicar a aparencia somente em `.progress-bar`.
- **Espacamento com chips precisa de valor explicito**: definir `margin-bottom` da progress evita variacao herdada do Bootstrap e permite ajuste fino (neste caso, `0px`).
- **Guardrail CSS dedicado reduz regressao visual**: esconder progresso dentro da dropzone (`display: none !important` no escopo `.upload-dropzone`) evita retorno acidental do problema em refactors futuros.
- **Mudancas de UX sensiveis exigem validacao visual por screenshot a cada iteracao**: em upload, pequenos detalhes de margem/borda alteram percepcao de qualidade e precisam de confirmacao rapida com imagem.

### CoordinateCleaner / Mapa

- **Convencao Natural Earth: numero menor = maior detalhe**: `scale = 10` (1:10M, ~1km) > `scale = 50` (1:50M, ~2km) > `scale = 110` (1:110M, ~10km). A documentacao do `cc_sea` diz o contrario ("Higher numbers equal higher detail") — esta errada.
- **Se o pacote precisa entregar `cc_sea(scale = 10)` "out of the box" via GitHub/CRAN-like install, nao dependa de download em runtime**: quando `rnaturalearthhires` nao pode entrar em `Imports`, a saida pratica e embutir um artefato derivado de `land 10m` em `inst/extdata` e carregar localmente em runtime.
- **Timeout fixo em validacao espacial transforma variacao de performance em falso erro funcional**: o `setTimeLimit(elapsed = 120)` parecia protecao, mas no WSL virou a causa direta do downgrade para `50m`. Tratar timeout como opt-in via env var e mais seguro do que codificar um limite implicito.
- **Para teste de mar, recorte a camada fisica por mascara geografica, nao por fronteira politica**: a primeira versao da referencia embutida usou `countries10` / `CONTINENT` para definir "Americas" e excluiu a Guiana Francesa por ela estar ligada a Franca no layer politico. `cc_sea` precisa de terra fisica, nao de soberania.
- **Cobertura embutida + fallback por ponto e melhor que fallback por rodada**: quando parte do dataset esta dentro da regiao suportada e parte nao, degrade apenas as linhas fora da cobertura. Rebaixar a execucao inteira para `50m` joga fora precisao exatamente onde ela mais importa.
- **`coverage_ref` buffered deve ser a fonte da verdade; bbox e apenas acelerador/fallback**: para decidir se um ponto pode usar a referencia embutida, um poligono de cobertura buffered e mais seguro do que um retangulo simples, especialmente perto de costa, Caribe e wraparound dos Aleutas.
- **Artefato espacial embutido precisa de teste territorial de regressao, nao so smoke test de carga**: validar que o `.rds` abre e e poligono nao basta. Inclua pontos reais de fronteira/coastline/territorios ultramarinos; a Guiana Francesa expôs um erro que passava por todos os testes estruturais.
- **Clustering do Leaflet (`markerClusterOptions`) esconde a posicao real dos pontos**: para diagnostico visual de coordenadas, pontos individuais sao essenciais. Usar raio adaptativo (menor para muitos pontos) + `preferCanvas = TRUE` para performance.
- **Nota de precisao visivel reduz confusao do usuario**: quando o motor de validacao usa dados simplificados, um chip de alerta (`alert-warning`) na legenda evita que o usuario descarte pontos costeiros legitimos.

## CSS Build / Modularizacao (Onda 5)

- **CSS monolitico deve ser fatorado por dominio com build deterministico**: ordenar modulos por numero (`00-tokens` a `16-help`) preserva cascata; script de build (`data-raw/build_css.R`) gera artefato final com header `GENERATED FILE`.
- **Guardrails de CSS bundle devem validar header e completude**: testar que `custom.css` comeca com header gerado e que todos os modulos em `css/` estao incluidos no bundle previne drift silencioso.
- **Contagem de linhas e smoke test minimo valido para CSS split**: `sum(modulos) + 1 header == bundle` confirma integridade sem render visual.

## i18n / Externalizar Dicionario (Onda 5)

- **Migrar dicionario i18n de R inline para JSON facilita manutencao e tooling externo**: `jsonlite::fromJSON` com `simplifyVector = FALSE` preserva estrutura de lista nomeada identica ao formato original.
- **BOM removal deve ser inline quando dependencia de load order nao e garantida**: em pacotes R, `data_dictionary.R` (d) carrega antes de `utils_io.R` (u); usar `sub("^\uFEFF", "", raw)` em vez de `strip_bom()` evita `could not find function` em tempo de source.
- **Cache in-process com `new.env(parent = emptyenv())` e padrao seguro para dados estaticos**: evita releitura do disco e isolamento via `emptyenv()` previne poluicao de namespace.
- **`.onLoad()` e o lugar correto para aquecer caches de dados estaticos do pacote**: chamar `load_i18n_dict()` em `.onLoad()` com `tryCatch` silencioso garante que o cache esteja pronto antes do primeiro `tr()` no build da UI; falha silenciosa preserva compatibilidade com ambientes sem o artefato (testes parciais, dev sem `inst/`).
- **Script one-shot de export (`data-raw/export_i18n.R`) nao deve fazer parte do build regular**: existe para migracao; alteracoes futuras devem ser feitas diretamente no JSON.

## Refatoracao de Modulos Shiny (Onda 5)

- **Ao extrair funcoes de `moduleServer` para arquivo separado, closures locais perdem escopo**: `custom_language_choices()` definida dentro de `mod_mapping_server()` nao e visivel em `mod_mapping_cards.R`. Inlinar a logica ou mover para utils.
- **Retorno de modulo como `list()` nomeada e superior a `attr()` em reactive**: auto-documentado, verificavel com `expect_named()`, e extensivel sem breaking change.
- **Ao refatorar modulo grande, limpar dead code no mesmo ciclo**: closures que deixam de ser chamadas apos extracao devem ser removidas imediatamente para evitar confusao.

## E2E / shinytest2 (Onda 5)

- **Testes E2E com `shinytest2` devem usar `skip_if_not_installed("shinytest2")`**: permite que a suite unitaria rode sem dependencia de chromote/browser.
- **Release gate com etapas numeradas explicita a ordem de verificacao**: unit -> guardrails -> integridade i18n -> E2E -> R CMD check cobre progressivamente do mais rapido ao mais lento.
- **Gate por env var e mais explicito que `skip_if_not_installed` para suites custosas**: `if (!identical(Sys.getenv("RUN_E2E"), "true")) skip(...)` garante que E2E nunca roda acidentalmente em `devtools::test()` mesmo com `shinytest2` instalado.
- **`AppDriver$new(app = shinyApp(ui, server))` e mais robusto que `app_dir`**: elimina dependencia de `app.R`/`server.R` existirem no diretorio raiz, instavel em ambientes de check.
- **Evitar `withr` em scripts de release gate**: `Sys.setenv()`/`Sys.unsetenv()` sao base R e nao exigem declaracao em DESCRIPTION; `withr::with_envvar()` quebraria o script em ambientes sem o pacote instalado.

## Rostrum / Ondas 0-1

- **Sampling de scoring precisa ser deterministico por coluna**: seed derivada do conteudo (`digest::digest2int`) + `withr::with_seed` elimina flutuacao entre execucoes.
- **Golden fixture de scoring e necessario para proteger regressao de formula**: sem baseline versionado, alteracao pequena em score passa despercebida.
- **Migracao SQLite deve rodar em `BEGIN IMMEDIATE` com rollback garantido**: `busy_timeout` sozinho nao impede estado parcial em mid-failure.
- **`schema_version` deve ser idempotente e monotono**: migracao repetida para a mesma versao nao pode inserir duplicado.
- **Evolucao de sinonimos V1->V2 precisa de adaptador explicito**: mapear `lang=any` para `language=mul` e preservar `name_score` como `confidence` evita perda de compatibilidade.
- **Com `renv`, novas dependencias no `DESCRIPTION` exigem sincronizacao imediata do lockfile**: sem isso, `load_all` e CI podem divergir entre ambientes.

## Rostrum / Onda 3

- **Composicao de Stage 2 precisa respeitar override manual antes de qualquer heuristica**: se `manual_overrides[[term]]` estiver preenchido, a composicao deve ser abortada para o termo.
- **Guard de circularidade deve ser baseado em lineage de termos, nao em coluna**: rastrear `composed_from` por termo evita loops indiretos (`A <- B` e depois `B <- A`).
- **Composicao de `scientificName` deve ser conservadora**: promover apenas para `SUGERIDO`, com explain detalhado e sem auto-aplicar composicao multi-coluna.
- **Composicao de `eventDate` precisa validar calendario real**: aceitar `2024-02-29` e rejeitar `2023-02-29` evita falso positivo silencioso.
- **Fallback para `verbatim*` funciona melhor como etapa pos-conflito (Stage 3.5)**: aplicado antes disso, pode influenciar indevidamente a escolha principal.
- **Fallback `verbatim*` nunca deve sobrescrever mapeamento existente do alvo**: usar condicao "target livre" reduz regressao em datasets com coluna verbatim ja mapeada.
- **Vetorizacao de parser row-wise traz ganho direto**: substituir loop por operacoes vetorizadas em `build_eventdate_interval()` reduz custo em cenarios 20k+ linhas.

## Rostrum / Ondas 4-5

- **Stage 3 so fica confiavel com criterio deterministico explicito**: ordenar por score sem desempate alfabetico final pode gerar variacao silenciosa entre execucoes.
- **Regra de ambiguidade precisa ser float-safe e centralizada**: usar `gap < ambiguity_gap - sqrt(eps)` em helper unico evita drift entre branchs de conflito.
- **Conflito de candidatos e fallback de perdedor devem ser desacoplados**: primeiro resolver vencedor/perdedor, depois aplicar `verbatim*` com guard (`score` minimo + alvo livre) reduz sobrescrita indevida.
- **Fallback pos-conflito pode mascarar teste de perdedor se nao houver bloqueio por termo**: quando um termo teve alternativas, bloquear fallback "primario" evita resultado enganoso no target `verbatim*`.
- **Aliases locais precisam de transacao em toda write (`BEGIN IMMEDIATE`)**: sem lock explicito, upsert e undo por sessao ficam sujeitos a corrida em uso multiusuario.
- **Precedencia por escopo deve ser aplicada no lookup, nao na escrita**: manter `personal > institution > public` no read path simplifica manutencao e permite coexistencia de regras.
- **`deprecated` deve filtrar no lookup, nao apagar historico**: preservar linha + evento auditavel viabiliza investigacao de regressao e rollback.
- **Batch undo por `run_id` fica pratico quando eventos e aliases compartilham chave operacional**: sem `run_id` indexado em eventos, rollback em lote vira scan custoso.
- **Alias que denota o conceito do taxon completo nunca deve apontar para o epiteto isolado**: "especie"/"species" mapeados para `specificEpithet` pareciam intuitivos, mas em datasets brasileiros a coluna "especie" quase sempre contem o binomial completo; o alias correto e `scientificName`. Aplicar o mesmo raciocinio a futuros aliases de nivel taxonomico: verificar se o nome da coluna denota o conceito completo (binomial, trinomial) ou apenas o componente (epiteto, autoria).
- **Alias ausente no bundle causa fallback ruim quando os tokens do nome da coluna nao coincidem com os tokens do termo DwC**: `"species"` nao aparecia no bundle; sem hit exato, o fallback e token-overlap entre `["species"]` e os tokens de `"scientificName"` = `["scientific", "name"]` -- zero tokens em comum, score 0.55. Qualquer alias de uso comum em cabecalhos de planilha deve aparecer explicitamente no bundle, mesmo que semanticamente obvio.

## Provedores BR (faunabr / florabr)

- **`verbose = FALSE` em `get_faunabr()` / `get_florabr()` impede o download**: ambos os pacotes condicionam o bloco de `httr::GET` a `if (verbose)`; o `utils::unzip()` roda incondicionalmente logo apos. Sempre passar `verbose = TRUE` internamente na camada wrapper do saira, desacoplando a verbosidade propria do saira da verbosidade dos pacotes externos.
- **Artifact check deve verificar o artefato final, nao intermediario**: `get_faunabr()` extrai `taxon.txt` do zip como passo intermediario e gera `CompleteBrazilianFauna.gz` como saida persistida; `load_faunabr()` le apenas o `.gz`. Verificar `taxon.txt` pode gerar falso negativo se o pacote limpar temporarios apos merge; verificar `CompleteBrazilianFauna.gz` e o contrato correto.
- **O nome do artefato final e o tratamento de `"latest"` sao contrato upstream que pode mudar entre versoes**: o `florabr` renomeou sua saida de `Flora_e_Funga_do_Brasil.rds` para `CompleteBrazilianFlora.rds`, quebrando o artifact check e deixando o provider preso em `update_failed`. Alem disso, `load_florabr()`/`load_faunabr()` deixaram de resolver `data_version = "latest"` para a pasta de versao baixada (ex.: `393.427/`), entao carregar logo apos um download `"latest"` falha com "cannot open the connection". Regra: depois do `get_*`, resolver `"latest"` para a versao real (`.brprovider_detect_downloaded_version()`) antes de chamar `load_*`, e manter o nome de artefato esperado casado com a saida do pacote atual. Ao bumpar `florabr`/`faunabr`, revalidar ambos os contratos com um download end-to-end.
- **Warnings de extracao de zip devem ser escalados a erros no wrapper**: `utils::unzip()` emite `warning()` (nao `stop()`) em falha de extracao; o `tryCatch(error=...)` do wrapper nao captura warnings. Usar `withCallingHandlers(warning=...)` para interceptar e converter em `stop()` com mensagem descritiva antes que o warning seja engolido.
- **Um pacote de provedor pode quebrar por mudanca no _formato dos dados_ na fonte, e isso nem sempre da pra contornar do wrapper**: o CTFB (Fauna do Brasil) passou a publicar a coluna `locality` vazia e com multiplas linhas por especie; o `merge_data()` interno do `faunabr` faz `if (!is.na(group$locality))` sobre um vetor, abortando com "the condition has length > 1" no R >= 4.2. Nao e codigo do saira nem da pra interceptar (funcao interna, rodada dentro de `load_faunabr()` sobre dados remotos). Solucao: pinar `faunabr` ao commit corrigido via `Remotes: github::wevertonbio/faunabr@<sha>` no DESCRIPTION (lido nativamente pelo `pak`, gravado no `renv.lock`), e reverter para o CRAN quando o fix sair. Licao: antes de assumir bug proprio, dumpar a funcao upstream (`get("merge_data", asNamespace("faunabr"))`) e checar o GitHub do pacote — o mantenedor ja tinha diagnosticado a `locality` vazia.
- **Um pin temporario a upstream tende a nao ser temporario, porque o gatilho de saida nao e o que se imagina** (2026-08-13): esse pin da `locality` foi criado em 0.9.2 com a condicao "reverter quando o fix chegar ao CRAN". Ele chegou: o commit pinado e ancestral do envio do 1.1.0 ao CRAN (`git merge-base --is-ancestor` confirma), publicado em 2026-07-02. Mesmo assim reverter era a escolha errada, porque o CTFB quebrou o formato **de novo** depois disso e o fix novo (1.1.1) so existia no GitHub. Ou seja: com uma fonte de dados que muda sozinha, "ja saiu no CRAN" nao e evidencia de que da pra despinar; a evidencia e um **download end-to-end funcionando** contra o dado publicado hoje. Regra pratica: ao revisitar um pin assim, comparar tres coisas -- o que o CRAN tem, o que o GitHub tem, e o que a fonte de dados esta publicando agora. E checar `last_error` no metadata do provider local: ele ja acusava "undefined columns selected" antes de qualquer investigacao.
- **`packageVersion()` nao distingue um build do CRAN de um do GitHub com a mesma versao**: o CRAN e o GitHub carregaram `1.0.1` ao mesmo tempo durante esse pin. So `RemoteSha`/`GithubSHA1` no DESCRIPTION instalado separam os dois (`read.dcf(system.file("DESCRIPTION", package = "faunabr"))`). Qualquer guarda automatica sobre pin deve olhar o SHA, nunca a versao.
- **Download de arquivo grande do IPT precisa de timeout explicito**: os arquivos da Flora/Fauna do Brasil sao grandes e o timeout default do R (`getOption("timeout")` = 60s) trunca o zip em conexao lenta, gerando o mesmo "error 1 in extracting from zip file" que parece falha de servidor. Elevar `options(timeout = max(old, 600))` com `on.exit(restore)` em `.brprovider_download_data_impl()` antes do `get_*` cobre os dois provedores.
- **O warning do dbplyr (`check_from argument of tbl_sql()`) e upstream e nao bloqueante**: emitido internamente pelo dbplyr >= 2.5.0 quando `taxadb` constroi queries SQL; nao e acionavel no codigo do saira. Suprimir com `withCallingHandlers` escopado nas chamadas a `taxadb::filter_name()` para manter console limpo sem mascarar outros warnings.
- **Diagnostico de download deve separar causa de sintoma**: "cannot open the connection" (sintoma) esconde "zip nao foi baixado porque verbose=FALSE" (causa). Ao investigar falha de provider, verificar primeiro se o arquivo `.zip` existe no tempdir antes de suspeitar do servidor remoto.
- **Bootstrap e refresh precisam de contratos diferentes**: primeiro download sem cache deve ser sincrono e falhar com erro claro; refresh de versao com cache existente deve ser assincrono para nao bloquear validacao.
- **Metadata por provider reduz regressao operacional**: persistir `status`, `local_version`, `last_checked_at`, `last_updated_at`, `last_error` e `retry_after_at` evita comportamento "caixa preta" em falhas intermitentes.
- **Lock em arquivo por provider evita corrida entre cliques/sessoes**: sem lock (`<provider>.update.lock`), dois disparos simultaneos podem sobrescrever cache e produzir estado inconsistente.
- **Escrita atomica e obrigatoria para cache grande**: salvar em `.tmp`, validar leitura, renomear para `.rds` e manter `.rds.bak` protege contra corrupcao parcial quando download/processamento falha no meio.
- **Falha de update nao deve invalidar cache local valido**: quando o refresh remoto falha, manter `.rds` anterior e marcar `update_failed` preserva continuidade para o usuario final.
- **`update_failed` com cache disponivel deve ser revertido para `up_to_date` na proxima leitura de status**: manter o badge de erro mesmo com dados usaveis confunde o usuario. A regra de reset deve ser simetrica a regra `never_downloaded → up_to_date`: se `has_data && status == "update_failed"`, reverter o status operacional (mantendo `last_error` para diagnostico).
- **Toda string visivel em funcoes de badge e notificacao deve usar `tr()`**: hardcodes de texto em ingles dentro de closures de modulo passam despercebidos em revisao de codigo por nao aparecerem no dicionario. Padrao: adicionar chave no `i18n.json`, chamar `tr("chave", lang_r())`, e incluir a chave na suite `test-utils-i18n.R` no mesmo ciclo.

## CRAN Submission

- **`Remotes:` proibido no CRAN**: mover dependencia nao-CRAN para `Suggests` +
  fallback gracioso (ou usar `Additional_repositories`). Nunca submeter com
  `Remotes:` no DESCRIPTION.
- **`LazyData: true` esta deprecated**: remover se nao houver artefatos em `data/`
  que precisem de lazy loading; `inst/extdata/` nao precisa de lazy.
- **`<<-` em handlers de `tryCatch`**: padrao correto e
  `result <- tryCatch(fn(), error = function(e) e)` seguido de
  `inherits(result, "error")`. Nao usar super-assignment para capturar erro.
- **`get(fn, envir = asNamespace("pkg"))` em producao**: acessar API publica
  explicitamente via `pkg::fn`. Reservar `:::` e `asNamespace` apenas para
  testes internos.
- **`@importFrom` obrigatorio para todas as dependencias**: centralizar em arquivo
  `R/saira-package.R` com bloco `## usethis namespace: start/end` e regenerar
  NAMESPACE com `devtools::document()`.
- **Funcoes exportadas exigem roxygen completo**: toda funcao com `@export` precisa
  de `@title`, `@description`, `@param`, `@return` e pelo menos um `@examples`.
- **`getFromNamespace()` nos testes**: substituir por `saira:::fn` para funcoes
  internas ou chamar diretamente para funcoes exportadas. Em loops dinamicos,
  usar `get(fn, envir = asNamespace("saira"), inherits = FALSE)`.
- **`launch.browser = TRUE` hardcoded**: substituir por
  `getOption("shiny.launch.browser", interactive())` para compatibilidade com
  ambientes headless (Docker, CI).
- **`.Rbuildignore` deve excluir artefatos de dev**: incluir `^\.claude$` e
  `^PLAN_.*\.md` para evitar incluir configs e planos de dev no tarball.

## Encoding / I/O

- **Amostragem parcial de encoding nao detecta bytes invalidos em linhas tardias**: `detect_encoding()` que amostra apenas as primeiras N linhas identifica incorretamente como UTF-8 arquivos com bytes Latin-1 depois da linha N. O padrao correto e validar o DataFrame pos-leitura com `iconv(col, "UTF-8", "UTF-8")` — NA inesperado indica byte invalido. Re-ler com encoding alternativo (`Latin1`, depois `Windows-1252`) resolve o caso; `readr` converte para UTF-8 ao ler com locale nao-UTF-8, entao nenhuma normalizacao adicional e necessaria.

- **Motor retornando `success = FALSE` nao deve sobrescrever estado de UI**: quando `run_rostrum_engine()` ou funcao analoga retorna falha interna, sempre verificar `engine_result$success` antes de propagar `engine_result$data` para `reactiveValues`. Propagar dado vazio de falha e equivalente a silenciar o erro para o usuario e apagar trabalho manual anterior. Padrao correto: checar `!isTRUE(success)`, emitir notificacao de erro e retornar cedo sem tocar em `rv`.

## DwC / Catalogo Sob Demanda (2026-05-06)

- **String vazia como chave de vetor nomeado em R lanca erro em `[[`**: `c("" = "valor")` cria vetor com chave vazia valida, mas acessar via `vec[["chave"]]` lanca "attempt to use zero-length variable name". Solucao: usar `list()` em vez de `c()` para o mapa, ou usar `vec["chave"]` (bracket simples) que retorna `NA` sem erro para chaves ausentes.

- **`[[` em vetor nomeado lanca erro para chave ausente, `[` retorna NA**: ao escrever helper de lookup em vetor nomeado com chaves possivelmente ausentes (ex: classe DwC desconhecida), usar `unname(vec[key])` e checar `is.na()` em vez de `vec[[key]]` e checar `is.null()`. A segunda forma lanca `subscript out of bounds` em producao.

- **`list[character_vector]` retorna lista, nao character**: ao usar lista nomeada como mapa de lookup e indexar com vetor de caracteres (`mapa[active$organized_in]`), o resultado e uma lista, nao um vetor. Para obter vetor, usar `vapply(values, function(x) mapa[[x]] %||% fallback, character(1))` ou equivalente.

- **Catalogo externo com termos duplicados entre namespaces exige deduplicacao explicita**: o TDWG `term_versions.csv` tem `language` nos namespaces `dc:` e `dcterms:` simultaneamente. Sem deduplicacao por prioridade de namespace, o catalogo exporta duas linhas com o mesmo `term_localName` e todos os consumidores (match, merge, wiki) falham silenciosamente com resultados duplicados.

- **Separar catalogo completo de conjunto-base ativo e melhor que inflar o conjunto-base**: adicionar todos os termos de um padrao externo ao conjunto-base do mapeamento polui a UI e pode quebrar testes que assumem tamanho fixo. O padrao correto e manter conjunto-base enxuto + catalogo separado, com helpers de composicao (`get_active_dwc_terms(extra)`) que sao transparentes para os consumidores.

- **Re-render reativo de `renderUI` e suficiente para adicionar cards dinamicamente em V1**: `insertUI` parece atraente para preservar scroll, mas exige seletores estaveis, lifecycle de binding e coordenacao com filtros. Quando o `rv` ja e a fonte de verdade (scroll recuperavel via `rv$map_values`), deixar o re-render reativo do `renderUI` acontecer e mais simples e mais robusto. Adotar `insertUI` apenas quando o custo de re-render se tornar mensuravel.

- **`all_filter_categories` como variavel estatica impede extensao dinamica**: ao migrar de `c("Record-level", ...)` hardcoded para `reactive({ unique(dwc_all()$category) })`, todos os observers que dependiam da variavel precisam ser atualizados para chamar `all_filter_categories()` com parenteses. Sem essa atualizacao, a sincronizacao do "select all" continua operando sobre o conjunto de categorias antigo — bug silencioso que so aparece quando o usuario adiciona um termo de classe nova.

- **Termos extras em auto_map devem ter confianca explicita reduzida**: o Rostrum engine foi calibrado sobre os 50 termos-base. Ao expandir `dwc_terms_df` com extras, o engine produz scores para eles, mas esses scores nao tem o mesmo significado que os do conjunto calibrado. O padrao correto e rebaixar `AUTO -> SUGERIDO` para extras no pos-processamento, nao no engine — isso preserva a semantica dos badges para o usuario e nao exige alteracao no engine.

- **Helper de pre-ativacao de termos de template deve ser puro e testavel**: a logica "quais termos deste template precisam ser ativados antes da aplicacao" e pura (catalogo + termos ativos + payload) e deve ficar em `utils_rostrum_templates.R`, nao no modulo Shiny. Isso permite testar o comportamento sem Shiny e reutilizar em diferentes pontos de aplicacao de template no futuro.

- **Exemplos literais de padroes externos nao devem ser exibidos sem tratamento**: o TDWG `term_versions.csv` armazena multiplos exemplos por termo em uma unica string separada por `;`, com markup de backtick, chegando a 768 chars. Copiar esse campo diretamente para exibicao em tabela torna a coluna inutilizavel. Para exibicao, truncar ao primeiro exemplo (antes do primeiro `;`), remover backticks, limitar a ~120 chars — ou omitir a coluna e redirecionar ao link do termo. Dados brutos permanecem no artefato para uso futuro.

- **`draw('page')` nao recalcula estado de paginacao apos mudanca de `page.len`**: em `applyPageLength` com filtro de coluna ativo, `table.page.len(len)` seguido de `table.page('first').draw('page')` produz resultado vazio porque `draw('page')` e um partial redraw que nao regenera o resultado paginado. Padrao correto: `table.page.len(len).draw(false)` — full redraw que respeita todos os filtros ativos e recalcula o subconjunto visivel com o novo tamanho de pagina.

- **Regra CSS adicionada apenas ao bundle e descartada silenciosamente na proxima regeneracao**: `custom.css` e artefato gerado por `data-raw/build_css.R` a partir dos modulos `inst/app/www/css/NN-*.css`. Qualquer regra editada direto no bundle final (ex.: hotfix em `custom.css` sem espelhar em modulo) some no proximo rebuild. Sintoma classico: a regra funciona por meses, ate alguem rodar o build script — entao a regressao reaparece. Toda regra **deve** viver no modulo de dominio correto; quando a regra for critica para UX (ex.: esconder fallback estatico de i18n para evitar texto duplicado no header), adicionar tambem guardrail em `test-css-guardrails.R` que falha se o seletor sumir do bundle.

- **Quando uma fonte canonica de dados expande (ex.: 50 -> 217 termos DwC; 6 -> 12 classes), todos os mapas auxiliares precisam ser auditados em conjunto**: i18n keys (`class_*`), CSS por slug (`.wiki-filter-pill[data-filter="..."]`, `.wiki-class-badge--{slug}`), JS lookup maps (`classSlugMap` em rowCallback), e qualquer hardcoded vector R (`class_tr_keys_map`). Fix parcial deixa lacunas visiveis ao usuario (nomes crus em ingles, badges com cor errada por fallback, filtros sem indicador visual). Antes de mergear a expansao, fazer um sweep de `grep -rn "Record-level\|Occurrence\|Event\|Location\|Identification\|Taxon"` para localizar todos os lugares onde a lista canonica esta replicada.

- **JS lookup map que depende de catalogo dinamico deve ser injetado a partir do R via `jsonlite::toJSON`, nao escrito como literal JS**: hardcodar `var classSlugMap = { 'record-level': 'record-level', ... }` em `DT::JS()` cria drift entre o lado R (que pode crescer com novos termos) e o lado JS (que fica estatico). Padrao seguro: construir a `list()` em R a partir do mesmo vetor canonico (`catalog_classes`) usado para gerar pills, slug map e i18n keys, e injetar via `sprintf("...var classSlugMap = %s; ...", jsonlite::toJSON(class_slug_js_map, auto_unbox = TRUE))` — mesma estrategia ja usada para `required_badge_labels` no mesmo arquivo.

- **Outputs ocultos via `display: none;` precisam de `outputOptions(suspendWhenHidden = FALSE)` explicito** (ADR-087). Shiny suspende outputs ocultos por padrao para economizar; isso significa que o valor (URL, no caso de `downloadButton`) nunca e enviado ao cliente. Sintoma classico: clicar num `downloadButton` escondido (wrapper `display:none;`, usado para acionar download via JS atras de um botao visivel mais bonito) baixa **a propria pagina HTML do app** com o nome `.csv` configurado em `filename`. Causa: `<a href="" download="dwc_export_xxx.csv">` resolve `href=""` para a URL atual, e o atributo `download` forca o nome. Fix: sempre que um `downloadButton`/`downloadLink` estiver dentro de wrapper escondido, adicionar `shiny::outputOptions(output, "<id>", suspendWhenHidden = FALSE)` apos o registro do handler. Ja existia o padrao em `mod_mapping.R:553` para `file_uploaded` — replicado para `download_real` em `mod_preview.R`.

- **`SAIRA_USER` unset cria mismatch silencioso entre `rostrum_upsert_alias` e `rostrum_apply_alias_overrides`** (ADR-087, bug encontrado na entrega do round-trip de aliases). O upsert promove `user_id = ""` -> `"anonymous"` antes de inserir (`R/utils_rostrum_db.R:470-472`), mas o lookup em `rostrum_list_aliases_for_column` **NAO** promove: `user_id_norm = NA_character_` faz o filtro de visibilidade `!is.na(user_id_norm) & ...` rejeitar todos os aliases personal. Resultado: aliases salvos via `rostrum_record_alias_override` (selecao manual) OU `import_mapping_guide_to_aliases` (round-trip do .txt) ficam invisiveis ao motor durante automap em qualquer sessao sem `SAIRA_USER` setado — i.e., o sistema todo de aliases personal estava quebrado por meses ate o usuario testar o round-trip e perceber que automap "ignorava" o .txt importado. Fix correto: promover `""` -> `"anonymous"` em ambos os lados (insercao E lookup). Aplicado em `rostrum_apply_alias_overrides` para alinhar. Quando criar par insert+lookup com normalizacao implicita (`""` -> sentinel), validar que ambos os lados aplicam a mesma promocao via teste end-to-end (insert -> lookup retorna a row).

- **Testes de modulo que usam `rostrum_connect()` default herdam estado do `~/.local/share/saira/rostrum.sqlite` real do dev** (ADR-087). `mod_mapping_server` abre `rostrum_connect()` sem path override, conectando no SQLite local persistente. Aliases que o desenvolvedor acumulou durante testing manual (via selecao de coluna ou import de .txt) leakam para os testes — automap em planilha de teste pode mapear colunas como `ALIAS` ao inves de `AUTO/SUGERIDO`, quebrando assertions sobre status. Fix: `withr::local_envvar(c(SAIRA_USER = paste0("test_isolation_", as.integer(Sys.time()))))` no inicio do teste isola por user_id unico — engine busca aliases sob esse id e nao encontra nenhum. Ate ter test infra para injetar conn temp em `mod_mapping_server`, esse padrao e necessario em qualquer teste que dispare `auto_map`.

- **Scroll para um elemento que so existe apos `renderUI` assincrono precisa de retry, nao de chamada unica** (ADR-089). Um observer que muda estado e logo dispara `session$sendCustomMessage(... scroll ...)` corre antes do `output$mapping_ui` re-renderizar — `document.getElementById(anchor)` retorna `null` e o scroll falha silenciosamente. Padrao robusto: handler inline com loop `requestAnimationFrame` limitado (ex.: cap ~40 frames), que tenta `getElementById` a cada frame, faz `scrollIntoView` quando achar e aborta gracioso apos o cap (sem scroll, mas sem travar). Sem acoplamento ao ciclo interno do Shiny; mesmo espirito dos handlers inline ja usados em `mod_preview.R`.

- **`position: sticky` so funciona se o elemento for filho direto do ancestral que tem o scroll** (ADR-089). A `.mapping-class-pillbar` gruda no topo porque foi inserida como **primeiro filho dentro** da `.mapping-scroll-container` (o elemento com `overflow-y:auto`). Colocar a barra no `bslib::card_header` (fora do container de scroll) ou mover o ancestral de scroll quebra o sticky sem erro nenhum — a barra simplesmente rola junto. Ao adicionar barras sticky em layouts bslib, confirmar quem e o ancestral de scroll antes de escolher onde inserir.

- **Previa de card deve exibir o valor *processado*, nao a coluna crua, ou o usuario ve dados inconsistentes**: o card de mapeamento mostrava os primeiros valores brutos da primeira coluna de origem (`preview_values_for_column(first_col)`), que podia ser uma coluna de mes ("September") enquanto a aba Preview mostrava o valor construido `build_eventdate_interval` ("2012-09/2016-02"). A inconsistencia confunde o usuario: parece que o dado vai errar. Fix: substituir `preview_values_for_column` por `processed_preview_for_term` que chama `build_term_value()` — a mesma logica de `build_processed_mapping_df` — aplicada sobre um head-slice do df (200 linhas). `build_term_value` foi extraida como funcao pura para ser reutilizada pelo card e pelo pipeline de exportacao sem duplicacao. Regra: o que aparece no card deve ser o que vai para o export.

- **`future::multisession` workers nao enxergam um pacote carregado via `pkgload::load_all()`**: workers multisession sao processos R frescos que tentam `loadNamespace("saira")` — mas sob `load_all` o pacote nao esta instalado, so carregado na sessao pai. Resultado: closures que referenciam funcoes internas do pacote falham no worker. Fix para testes: usar `future::multicore` (fork herda o namespace, funciona no Linux/macOS) e guardar o teste com `skip_if(pkgload::is_dev_package("saira"), ...)` para o caso multisession (que deve rodar apenas sob `R CMD check` com pacote instalado). Ver ADR-091.

- **`format(n, big.mark = ".")` emite aviso quando `decimal.mark` padrao tambem e `"."`**: R avisa `'big.mark' and 'decimal.mark' are both '.'`. Ao formatar inteiros com separador de milhar no estilo PT-BR (ponto), sempre passar `decimal.mark = ","` explicitamente (ou usar o helper `format_count(n, lang)`). Sem o `decimal.mark` explicito o aviso vaza para os testes via `expect_snapshot` ou `devtools::test()`.

- **Warnings intencionais de producao devem ser afirmados com `expect_warning()`, nao suprimidos**: quando um teste exercita um path de recuperacao ou falha que emite `warning()` intencionalmente (ex.: encoding retry em `utils_io.R`, download falho em `utils_brproviders.R`), usar `expect_warning(expr, "parte da mensagem")` — isso (a) remove o ruido da suite, (b) torna o contrato explodindo se o warning sumir, (c) serve como documentacao viva do comportamento esperado. Suprimir com `suppressWarnings` omite a afirmacao do contrato.

- **Mascaramento de coordenadas deve respeitar generalizacao upstream do publisher** (ADR-095). `mask_sensitive_coordinates()` ja sobrescrevia incondicionalmente quaisquer linhas de especies sensiveis — empilhando generalizacao sobre dado ja generalizado e descartando `dataGeneralizations` / `informationWithheld` que o publisher tinha populado. Gate adotado: `coordinateUncertaintyInMeters >= 1000` **OR** `dataGeneralizations` nao-vazio -> skip total da linha (preserva coords + metadados upstream; nao entra em `result$real` porque nao temos os originais). A escolha do OR-gate veio depois de ler o PDF do `camtrapdp` v0.5.0 p.26 (`write_dwc()` popula `dwc:dataGeneralizations` *por contrato* quando `coordinatePrecision` esta definido) e p.18-19 (`round_coordinates(x, 3)` gera uncertainty ~150m — abaixo do threshold numerico mas com `dataGeneralizations` populado). Sem o segundo sinal, re-mascarariamos Chapman categoria 4 e sobrescreveriamos a generalizacao do publisher silenciosamente. `informationWithheld` ficou de fora por ora — sinal menos consistente entre publishers. Fixtures de teste com valores incidentais (`coordinateUncertaintyInMeters = "3000"` em `make_df()`) que cruzaram o limiar tiveram que ser ajustadas — ao adicionar um novo gate de bypass, varrer fixtures compartilhadas (`grep -n coordinateUncertaintyInMeters tests/`) e ajustar valores incidentais para nao colidir com o gate.

- **Ler o PDF / vignette oficial antes de implementar contra pacote externo** (ADR-095). A v1 do Camtrap DP foi escrita com base em conhecimento publico do padrao e da API do `camtrapdp` — pareceu suficiente. A leitura tardia do PDF v0.5.0 do pacote revelou tres contratos que mudaram o codigo: (1) `write_dwc()` retorna invisivelmente os dataframes transformados (p.25) — eliminou o passo de reler CSV do disco; (2) `write_dwc()` filtra observacoes para `observationType = "animal"` (p.25) — a mensagem de sucesso precisou explicitar a filtragem para o usuario nao se confundir com contagens menores que o pacote upstream; (3) `dataGeneralizations` e populado por contrato quando ha generalizacao (p.26) — o gate de skip de remask passou a ser OR-gate em vez de threshold-only. Custo: ~15 minutos extras de leitura. Beneficio: 3 correcoes cirurgicas em vez de 3 issues abertos depois pelos usuarios. Regra: sempre que adicionar um pacote em Imports/Suggests, abrir o manual oficial e ler pelo menos as funcoes que vamos chamar; checar `Value`, `Details`, e `See Also` — frequentemente ha um caminho mais barato ou um contrato sutil que o conhecimento publico nao captura.

- **Dependencias R com requisito de sistema (ex.: `libjq-dev` para `jqr`) devem viver em `Suggests`, nunca em `Imports`** (ADR-095). O `camtrapdp` arrasta `EML` -> `jqr` -> precisa de `libjq-dev` instalado no sistema (apt). Em maquinas sem essa lib, `install.packages("camtrapdp")` falha em cascata silenciosamente para quem nao le os warnings. Mesmo padrao ja usado para `rnaturalearthhires`: declaramos como Suggests, guardamos cada chamada com `requireNamespace(..., quietly = TRUE)`, e emitimos uma mensagem i18n acionavel ("Rode install.packages('...')") em vez de um stack trace cru. Sintoma classico se for para Imports: usuarios reportam "Saira nao instala" em CI/dev limpos; o erro real esta 5 telas acima na cascata de deps.

- **Validar zip de upload por *conteudo* (presenca de descriptor), nao so por extensao** (ADR-095). O `is_camtrap_dp_zip()` abre o zip com `utils::unzip(path, list = TRUE)` e verifica `"datapackage.json" %in% basename(Name)`. Validar por extensao `.zip` apenas e fragil — usuario pode mandar qualquer zip e o erro so aparece dentro de `camtrapdp::read_camtrapdp()` com mensagem cripitica. Padrao: probe estrutural barato (listing do zip) na primeira camada, mensagem clara via `tr("err_camtrap_invalid_zip", lang)`, e a chamada cara (descompactar + parsear) so apos validar a estrutura. Reusa o mesmo padrao do `is_saira_mapping_guide()` (ADR-087) que valida magic header antes de tentar parse — economiza tempo do usuario e nao polui o log com erros de parse de arquivo invalido.

- **Descritor Frictionless sintetico: `profile` do recurso deve ser a string literal `"tabular-data-resource"`, nao uma URL de schema; e o CSV precisa ter *todas* as colunas canonicas** (ADR-096). Ao gerar um `datapackage.json` em memoria para que `camtrapdp::read_camtrapdp()` aceite CSVs soltos, cada entrada em `resources` precisa de `"profile": "tabular-data-resource"` (literal exigido pelo Frictionless) — o URL do schema vai em `"schema"`, nao em `"profile"`. Colocar o URL em `profile` faz `frictionless::read_resource()` rejeitar o recurso com "must have a profile property with value 'tabular-data-resource'". Segundo ponto: `frictionless::read_resource()` aplica `col_select` estritamente a partir do schema e **descarta silenciosamente** colunas extras ou ausentes antes de passar o tibble para `camtrapdp::write_dwc()`. Se o CSV normalizado tiver menos colunas que o schema canonico (mesmo que sejam todas `NA`), `write_dwc()` falha com "Column X not found in .data". Fix: gerar todos os dataframes (deployments/media/observations) com a lista completa de colunas canonicas do Camtrap DP 1.0.2, preenchendo `NA` nas nao-mapeadas. A lista definitiva vem de `camtrapdp::write_camtrapdp(example_dataset(), directory = tmp)` + inspecao dos headers gerados — nao de memoria ou documentacao parcial.

- **Wildlife Insights e Camtrap DP sao schemas completamente diferentes apesar do dominio comum** (ADR-096). WI usa snake_case proprietario (`is_blank`, `wi_taxon_id`, `cv_confidence`, `number_of_objects`, `identified_by`); Camtrap DP usa camelCase padrao (`observationType`, `scientificName`, `classificationProbability`, `classificationMethod`). A deteccao por assinatura de arquivos e confivel: WI sempre tem `cameras.csv` + `projects.csv` + pelo menos um `images_<project_id>.csv`; Camtrap DP solto tem `observations.csv`. Nao depender so de `deployments.csv` para distinguir — ambos os formatos tem esse arquivo. A derivacao de `observationType` precisa de regra explicita: `is_blank == 1` → `"blank"`; `genus == "Homo"` → `"human"`; `class == "Vehicle"` ou `common_name == "Vehicle"` → `"vehicle"`; taxonomy ausente → `"unknown"`; caso contrario → `"animal"`. O campo `cv_confidence` do WI varia entre 0-100 (precisa dividir por 100) ou 0-1 dependendo da versao do export — verificar o max antes de normalizar (`if (max(x, na.rm = TRUE) > 1) x / 100`). Ao adicionar suporte a novo formato de entrada, sempre mapear para o schema intermediario mais proximo que o pipeline ja processa (aqui: Camtrap DP) em vez de criar uma rota paralela ate o DwC — reduz superficie de manutencao.

- **Nao envolver strings de `tr()` em `enc2utf8()` antes de `writeLines`** (ADR-098). O CSV privado de coordenadas reais saia com a primeira linha em mojibake (`# Arquivo privado â€" ...`) porque `mod_preview.R` fazia `writeLines(enc2utf8(c(comentario, csv)), path, useBytes = TRUE)`. As strings vindas de `tr()` (i18n.json lido com `encoding = "UTF-8"`) ja chegam como bytes UTF-8; `enc2utf8()` aplicado a uma string com marca de encoding incorreta re-codifica os bytes e duplica a codificacao. O arquivo irmao `mapping_guide_*.txt` usa o mesmo `writeLines(..., useBytes = TRUE)` SEM `enc2utf8()` e sai correto. Regra: com `useBytes = TRUE` os bytes sao gravados crus — nao aplicar `enc2utf8()`/`iconv()` antes; deixar a string como `tr()` a entrega.

- **`renderUI` que recria widgets de input nao pode depender de estado reativo que observers desses inputs mutam** (ADR-098). Digitar no campo `datasetName` da aba de mapeamento congelava o app: `output$mapping_ui` (renderUI) lia `rv$map_meta`, e `observeEvent(input$custom_datasetName)` reescrevia `rv$map_meta` a cada tecla. Cada escrita re-executava o renderUI, que recria via `build_field_card()` todos os widgets de input; os inputs recriados sao re-vinculados no navegador e re-disparam seus observers — realimentacao infinita que trava a UI. Fix cirurgico: isolar a leitura (`shiny::isolate(rv$map_meta[[term]])`) no construtor do card. Regra: se um `renderUI` recria inputs, ele nao pode ter dependencia reativa em estado que observers desses mesmos inputs escrevem; isole a leitura, ou separe a renderizacao do widget da renderizacao do estado derivado (badge/classe).

- **Isolar identidade nao isola o banco: teste que escreve em SQLite precisa de `SAIRA_DATA_DIR`, nao so de `SAIRA_USER`** (ADR-115). Dois testes em `test-mod-mapping-server.R` setavam `SAIRA_USER = paste0("test_isolation_", ...)` com o comentario correto de que era para nao ler os aliases reais do desenvolvedor. Funcionava para **leitura**: o lookup filtra por `user_id`, entao os aliases do usuario ficavam invisiveis. Nao fazia nada pela **escrita**: `rostrum_connect()` resolve o arquivo por `SAIRA_DATA_DIR` -> `tools::R_user_dir("saira", "data")`, entao cada rodada de `devtools::test()` gravava duas linhas novas no `rostrum.sqlite` real, cada uma sob um `user_id` inedito. Descoberto com 566 aliases no banco, dos quais **376 eram residuo de teste** (`taxon name -> scientificName` e `tipo -> type`, sob 377 `user_id` distintos) e 15.980 eventos. Sintoma que nao aparece: as linhas ficam inertes para o auto-map justamente porque o `user_id` nao casa, entao nada quebra -- so cresce. Correcao: `tests/testthat/setup.R` aponta `SAIRA_DATA_DIR` para `withr::local_tempdir()` na suite inteira (o `setup.R` roda antes de todo arquivo de teste, coisa que `withr::local_envvar` por teste nunca alcanca), mais um teste-guarda afirmando que `rostrum_normalize_db_path()` resolve dentro de `tempdir()`. Regra geral: quando o codigo persiste fora do projeto, o isolamento tem de mirar o **caminho**, e o teste-guarda tem de afirmar o caminho -- afirmar o comportamento nao detecta vazamento, porque vazamento nao muda comportamento.

- **Medir antes de decidir a causa: o gate de aba estava certo e nao era a causa** (ADR-114 vs ADR-116). A tela de mapeamento travava ~65s e a primeira hipotese -- observers avidos puxando o dataset inteiro de uma aba escondida -- era verdadeira, cirurgica e **quase irrelevante**: com o gate aplicado o fluxo caiu de 71,9s para 71,3s. So depois de reproduzir em harness com a planilha real do usuario e rodar `Rprof` e que apareceu o culpado, com 71,66% do tempo em `split_output_tokens` -- o laco por linha do ramo multi-coluna de `collapse_mapped_values()`. Duas licoes praticas: (1) quando um consumidor caro e chamado N vezes, atacar o N antes de medir o custo unitario e apostar as a cegas -- perfilar decide qual dos dois domina; (2) `shiny::testServer` renderiza **todos** os outputs, porque nao ha cliente reportando visibilidade, entao ele e um limite superior pessimista de quantas vezes um output caro roda, nunca prova de que o navegador faz o mesmo. Para contar invalidacoes de verdade num harness, instrumentar a funcao cara com `assignInNamespace()` e imprimir `sys.calls()` filtrado por `utils::getSrcFilename()` -- foi isso que apontou a linha exata (`mod_sensitive_coords.R:404`) que as buscas por texto tinham deixado passar.

- **Isolar identidade nao isola o banco: teste que escreve em SQLite precisa de `SAIRA_DATA_DIR`, nao so de `SAIRA_USER`** (ADR-115). Dois testes em `test-mod-mapping-server.R` setavam `SAIRA_USER = paste0("test_isolation_", ...)` com o comentario correto de que era para nao ler os aliases reais do desenvolvedor. Funcionava para **leitura**: o lookup filtra por `user_id`, entao os aliases do usuario ficavam invisiveis. Nao fazia nada pela **escrita**: `rostrum_connect()` resolve o arquivo por `SAIRA_DATA_DIR` -> `tools::R_user_dir("saira", "data")`, entao cada rodada de `devtools::test()` gravava duas linhas novas no `rostrum.sqlite` real, cada uma sob um `user_id` inedito. Descoberto com 566 aliases no banco, dos quais **376 eram residuo de teste** (`taxon name -> scientificName` e `tipo -> type`, sob 377 `user_id` distintos) e 15.980 eventos. Sintoma que nao aparece: as linhas ficam inertes para o auto-map justamente porque o `user_id` nao casa, entao nada quebra -- so cresce. Correcao: `tests/testthat/setup.R` aponta `SAIRA_DATA_DIR` para `withr::local_tempdir()` na suite inteira (o `setup.R` roda antes de todo arquivo de teste, coisa que `withr::local_envvar` por teste nunca alcanca), mais um teste-guarda afirmando que `rostrum_normalize_db_path()` resolve dentro de `tempdir()`. Regra geral: quando o codigo persiste fora do projeto, o isolamento tem de mirar o **caminho**, e o teste-guarda tem de afirmar o caminho -- afirmar o comportamento nao detecta vazamento, porque vazamento nao muda comportamento.

- **Mapeamento gravado em `map_values` que o pipeline nao le produz um guia que mente sobre o proprio arquivo** (ADR-118). `resolve_occurrence_ids()` procurava a coluna literal `occurrenceID` e ignorava `map_values`, entao o mapeamento `<coluna do usuario> -> occurrenceID` era escrito, exibido no guia exportado e descartado na hora de gerar os IDs. O erro sobreviveu porque cada camada estava internamente coerente: o guia lia `map_values` (e acertava), o pipeline lia a coluna crua (e "acertava" pelo seu proprio criterio), e o export via a coluna cheia e concluia `user_supplied`. Nenhum teste falhava porque nenhum atravessava as tres. Duas regras: (1) para termo com tratamento especial, a fonte da verdade tem de ser **uma** -- se o guia e o pipeline resolvem a coluna por caminhos diferentes, eles vao divergir; (2) o rotulo declarativo (`id_strategy = "user_supplied"`) e caro justamente por ser barato de emitir -- publicar a **contagem** ao lado (`N de M preservados`) transforma a afirmacao em algo que o leitor consegue conferir contra o arquivo. Ver tambem: `occurrenceID` estava em `special_no_dropdown`, entao o unico caminho que gravava o mapeamento era a importacao de modelo, que nao aplica aquela exclusao -- uma exclusao aplicada em 3 de 4 caminhos e um bug esperando data.

- **Chave natural plausivel quase nunca e unica, e o requisito nao era chave nenhuma** (ADR-118). Testando uma ancora candidata (`eventDate + basisOfRecord`) numa planilha real de 42.528 registros: 2.738 valores unicos, 39.790 colisoes. Campos descritivos repetem aos milhares, e datasets agregados contem linhas identicas em **todo** campo publicado vindas de estudos-fonte distintos, entao nem as 32 colunas DwC concatenadas resolveram (5.814 colisoes). Constante do dataset nunca desempata: so desloca tudo em bloco. Mas a licao maior veio depois de eu implementar um seletor de chave natural e o usuario perguntar o obvio: se ele escolher **uma** coluna que ja identifica a linha, para que derivar um identificador dela? Nao serve para nada -- o certo e usar a coluna. O seletor inteiro era complexidade acidental cobrando do usuario uma decisao que os dados ja respondem. Regra: antes de construir configuracao, verificar se a regra de negocio nao e uma cadeia de precedencia fixa (valor existente > gerado). Se for, a configuracao e uma pergunta sem resposta errada possivel, ou seja, uma pergunta que nao precisava ser feita.

- **Separar o requisito da implementacao no vocabulario, nao so no codigo** (ADR-118). O requisito e "identificador que permanece igual entre exportacoes"; UUID e so uma implementacao. Escrevi UUID em cinco lugares visiveis (nome de funcao, rotulo de estrategia, i18n, prefixo no valor, texto do guia), amarrando o produto a uma escolha tecnica que deveria ser trocavel por ULID ou outro digest sem ninguem perceber. Corolario tecnico que reforca o ponto: UUID v5 **e** SHA-1 sobre (namespace + nome), entao nao existe passo de hash separado -- a string montada entra como *nome*. E como o algoritmo nao exige coluna nenhuma (`UUIDfromName(ns, "qualquer texto")` e v5 valido), toda a utilidade vem da string de entrada: alimentado pelo numero da linha, o ID e estavel entre reexportacoes identicas e quebra na primeira insercao, deslocando cada registro para o ID do vizinho -- pior que aleatorio, porque parece resolvido.

- **Identificador deterministico por conteudo precisa de desempate, senao colide onde mais importa** (ADR-118). Derivar o ID do conteudo da linha entrega o que o usuario quer (reenviar a planilha reproduz os IDs), mas dataset agregado contem legitimamente linhas identicas em **todo** campo publicado: mesmo taxon, mesmo ponto, mesma data, vindos de estudos-fonte diferentes. Medido: 5.814 linhas identicas em 42.528. Hash puro colidiria e o GBIF recusa `occurrenceID` duplicado. A saida foi numerar a n-esima ocorrencia dentro do grupo de conteudo igual, com o custo declarado em vez de escondido (corrigir um valor muda o ID daquela linha). Dois detalhes que parecem menores e nao sao: as partes tem de ser unidas por separador que nao ocorre em celula (`("ab","c")` e `("a","bc")` colidem numa concatenacao simples), e a coluna de identificador nunca pode alimentar o proprio hash, senao a mesma linha hasheia diferente conforme a coluna esteja ausente ou presente-e-vazia.

- **Antes de otimizar um laco, verificar se a funcao ja e vetorizada** (ADR-118). `uuid::UUIDfromName()` aceita vetor: 42.528 identificadores levam 0,023s numa chamada contra 1,9s no `vapply` equivalente, ~80x, com resultado identico verificado por `identical()`. Vale para a numeracao dentro de grupos tambem: `ave(FUN = seq_along)` e a forma legivel mas particiona por valor distinto, o que em coluna quase toda unica cria uma lista de dezenas de milhares de grupos; uma unica ordenacao faz o mesmo trabalho.

- **Uma exclusao replicada em N lugares so esta removida quando os N sairem** (ADR-118). Tirar `occurrenceID` de `special_fields` e `special_no_dropdown` em `mod_mapping.R` parecia resolver, e o teste de card passava. Faltou `manual_only_terms` no scoring do Rostrum (`utils_mapping.R`), entao a coluna chamada exatamente `occurrenceID` seguia saindo `MANUAL` e exigia selecao manual. O sintoma so aparece exercitando o caminho de ponta a ponta: o unitario do card testava a UI, o do resolver testava a regra, e nenhum dos dois passava pelo motor que decide se o termo e sequer pontuado. Ao remover um termo de uma lista de excecao, procurar a lista pelo *conteudo* (`rg "occurrenceID"` em `R/`) em vez de pelo nome da variavel -- as quatro tinham nomes diferentes.

- **O aviso e codigo tambem, e diverge do arquivo do mesmo jeito** (ADR-118/120). `unmapped_raw_columns()` foi extraida justamente para que a tela e o export nao discordassem sobre quais colunas ficam de fora, e mesmo assim a tela chamou sem o argumento `exclude` que o export passa. Resultado: a interface acusava `occurrenceID` como coluna que o GBIF ignoraria, enquanto o arquivo a declarava como campo `id` do `meta.xml`. Extrair a funcao compartilhada nao basta se os chamadores podem passar argumentos diferentes; ou o default e o comportamento correto, ou o teste tem de comparar as duas chamadas.

- **Capitalizacao e o unico sinal que separa epiteto infraespecifico de autoria** (ADR-119). `Dasypus septemcinctus hybridus` e `Dasypus novemcinctus Linnaeus` tem a mesma forma: tres tokens. O normalizador de token existente (`format_epithet_token()`) nao serve de juiz porque faz `tolower()` **antes** de validar contra `^[a-z][a-z-]*$`, entao `Linnaeus` passa. A checagem tem de olhar o token cru. Casos que a guarda precisa recusar e um regex ingenuo aceita: `(Linnaeus, 1758)`, `L.`, `Schinz 1825`.

- **Preservar coluna no arquivo nao e publicar a coluna** (ADR-120). O DwC-A preservava colunas nao mapeadas no fim do CSV, mas o `meta.xml` so declara `<field>` para termo reconhecido, e o GBIF le o `meta.xml`. As colunas ficavam fisicamente presentes e invisiveis no dataset publicado, o que anula o objetivo. Regra: em formato com manifesto (DwC-A, Frictionless, Parquet+schema), "esta no arquivo" e "esta no dataset" sao afirmacoes diferentes; se o produto so garante a primeira, ele tem de dizer isso onde o usuario ainda pode agir.
