% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_export.R
\name{build_eml_xml}
\alias{build_eml_xml}
\title{Build an EML 2.1.1 dataset descriptor for a Saira export}
\usage{
build_eml_xml(df, metadata = list(), package_id = NULL)
}
\arguments{
\item{df}{Data frame that will become `occurrence.txt`.}

\item{metadata}{Named list with optional entries: `title` (string),
`creator` (named list with `name`, `email`, `organization`), `license`
(string, default `"CC0-1.0"`), `abstract` (string).}

\item{package_id}{Optional UUID for the EML packageId; defaults to a
freshly generated UUID.}
}
\value{
Character scalar containing the serialized EML document.
}
\description{
Produces a minimal-but-valid EML document with required GBIF/IPT fields:
title, creator, contact, pubDate, abstract, intellectualRights, and
coverage (geographic + temporal). Geographic and temporal coverage are
computed automatically from the data via `compute_dataset_extents()`;
callers supply the editable fields (title/creator/license/abstract) via
`metadata`.
}
