% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_export.R
\name{process_for_export_with_unmapped}
\alias{process_for_export_with_unmapped}
\title{Append non-mapped raw columns to processed export}
\usage{
process_for_export_with_unmapped(
  df_processed,
  raw_data,
  map_values,
  overridden_terms = character(0)
)
}
\arguments{
\item{df_processed}{Data frame already produced by build_processed_mapping_df()
(the DwC-shaped intermediate, BEFORE process_for_export()).}

\item{raw_data}{Data frame with original uploaded columns.}

\item{map_values}{Named list keyed by DwC term, values are char vectors of
source column names.}

\item{overridden_terms}{Character vector of terms whose fixed value won over
their column mapping (`overridden_mapping_terms()`).}
}
\value{
Data frame: process_for_export(df_processed) + tail of unused raw cols.
}
\description{
Wraps process_for_export(): runs the existing pipeline, then appends to
the right side any column from `raw_data` that the user did not select
as a source for any DwC mapping. Preserves original names and order so
the user can hand-edit dropped fields after IPT upload.
}
