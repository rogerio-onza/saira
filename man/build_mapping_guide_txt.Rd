% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_export.R
\name{build_mapping_guide_txt}
\alias{build_mapping_guide_txt}
\title{Build dual-purpose mapping guide TXT (humano + maquina)}
\usage{
build_mapping_guide_txt(
  map_values,
  raw_data,
  lang = "pt",
  required_terms = c("scientificName", "eventDate", "decimalLatitude",
    "decimalLongitude", "basisOfRecord"),
  source_file = NA_character_
)
}
\arguments{
\item{map_values}{Named list: nome = termo DwC, valor = char vec de colunas
fonte (multi-coluna serializa como "colA + colB").}

\item{raw_data}{data.frame original (para listar colunas nao usadas).}

\item{lang}{Character scalar "pt" ou "en" (ou reactive — extraido com `()` se for).}

\item{required_terms}{Character vector de termos DwC obrigatorios (default = required do preview).}

\item{source_file}{Optional character: nome do arquivo de origem para metadados.}
}
\value{
character vector (uma entrada por linha), encoding UTF-8.
}
\description{
Texto plano que serve simultaneamente como (a) relatorio legivel para o
usuario e (b) entrada importavel pelo upload do Saira. NAO contem dados,
apenas o vocabulario "Coluna -> Termo". Cada linha de mapping vira uma
row em rostrum_aliases ao re-importar via parse_mapping_guide_txt().
}
\details{
Header magico fixo: `# saira:mapping:v1` na primeira linha.
}
