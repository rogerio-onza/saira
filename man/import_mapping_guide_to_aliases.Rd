% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_rostrum_templates.R
\name{import_mapping_guide_to_aliases}
\alias{import_mapping_guide_to_aliases}
\title{Import a parsed mapping guide into rostrum_aliases}
\usage{
import_mapping_guide_to_aliases(
  payload,
  conn = NULL,
  scope = "personal",
  user_id = NULL
)
}
\arguments{
\item{payload}{Output of \code{parse_mapping_guide_txt()}.}

\item{conn}{Optional open DBI connection. When NULL (default), a new
connection is opened via \code{rostrum_connect()} and closed on exit.}

\item{scope}{Character, default "personal".}

\item{user_id}{Character, default \code{Sys.getenv("SAIRA_USER", unset = "anonymous")}.}
}
\value{
Invisibly: integer count of (source, term) pairs upserted.
}
\description{
For each pair in \code{payload$pairs}, calls \code{rostrum_upsert_alias()}
with \code{scope = "personal"}, \code{confidence = 1.0}, \code{reviewed = TRUE}.
Re-importing the same guide updates rows in place (no duplicates) thanks
to the SELECT-then-INSERT/UPDATE logic of \code{rostrum_upsert_alias()}.
}
\details{
Multi-column source entries (e.g., produced by \code{build_mapping_guide_txt()}
as \code{"colA + colB -> term"}) are split on \code{" + "} and inserted as
independent aliases, one per source column.
}
