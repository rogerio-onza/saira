% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_coords.R
\name{coords_transposed_corrections}
\alias{coords_transposed_corrections}
\title{Detect and correct transposed / sign-flipped geographic coordinates}
\usage{
coords_transposed_corrections(
  df,
  lat_col = "decimalLatitude",
  lon_col = "decimalLongitude",
  country_col = "country",
  country_iso3 = NULL,
  border_buffer = 0.2,
  ref = NULL,
  iso_col = "iso_a3_eh"
)
}
\arguments{
\item{df}{data.frame with coordinate and country columns.}

\item{lat_col, lon_col, country_col}{Column names.}

\item{country_iso3}{Optional precomputed ISO3 vector (else resolved from
\code{country_col} via \code{coords_country_to_iso3()}).}

\item{border_buffer}{Numeric degrees. Records within this distance of the
informed country border are left untouched. Default 0.2 (~22 km).}

\item{ref}{Optional country SpatVector (else \code{coords_load_ne_land(50)}).}

\item{iso_col}{ISO3 attribute column name in \code{ref}.}
}
\value{
Named list: \code{corrected} (logical), \code{lat_new}/\code{lon_new}
  (numeric, equal to originals where not corrected), \code{transform}
  (character key or NA), \code{informed_country} (ISO3), \code{n_corrected},
  \code{n_candidates}, \code{available} (FALSE when no country layer could be
  loaded, e.g. offline).
}
\description{
For records whose verbatim coordinates fall outside the informed country (plus
an optional border buffer), tries latitude/longitude swap and sign-flip
transformations on Saira's bundled Natural Earth country layer and accepts the
first that lands inside the country. Verbatim coordinates are never mutated
here — corrections are returned alongside the originals so the caller (UI) can
preview and apply them explicitly.
}
