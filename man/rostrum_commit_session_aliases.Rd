% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_rostrum_db.R
\name{rostrum_commit_session_aliases}
\alias{rostrum_commit_session_aliases}
\title{Commit a Confirmed Mapping to the Alias Store}
\usage{
rostrum_commit_session_aliases(
  conn,
  map_values,
  run_id = NULL,
  scope = "personal",
  created_by = Sys.getenv("SAIRA_USER", unset = ""),
  user_id = Sys.getenv("SAIRA_USER", unset = ""),
  institution_id = Sys.getenv("SAIRA_INSTITUTION", unset = "")
)
}
\arguments{
\item{conn}{A DBI connection from \code{rostrum_connect()}.}

\item{map_values}{Named list of the final mapping: names are Darwin Core
terms, values are the source column(s). Terms with no column, or resolved
from a fixed value rather than a column, are skipped.}

\item{run_id}{Optional run identifier string shared by the whole batch.}

\item{scope}{Character. One of \code{"personal"}, \code{"institution"}, \code{"public"}.}

\item{created_by}{Character. User identifier.}

\item{user_id}{Character. User identifier for scope resolution.}

\item{institution_id}{Character. Institution identifier.}
}
\value{
Invisibly, a data frame of the committed pairs (zero rows when there
  was nothing to learn).
}
\description{
Learns column-to-term aliases from a mapping the user has confirmed by
exporting. Saira deliberately does not learn from selections made while
mapping: an intermediate pick is a hypothesis, and recording hypotheses is
what filled the store with entries like \code{id -> basisOfRecord} left
behind by cycling through options on a card.
}
\details{
The whole set is written in a single transaction under one \code{run_id}, so
\code{undo_session_aliases()} can reverse exactly one export.
}
